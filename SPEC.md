# 黒本デジタル化システム 仕様書

> **この文書の位置づけ**
> 本書は完成形の仕様を定義する。実装方法は記述しない（→ `CLAUDE.md`）。
> 決定の経緯は記述しない（→ `DECISIONS.md`）。
> **本書はエージェントによる編集を禁止する。** 実装と本書に相違がある場合は、
> 実装を修正するか、相違を報告して指示を待つこと。

---

## 1. 目的と利用シーン

### 1.1 対象資料

国土交通省航空局『航空保安業務処理規程 第５管制業務処理規程』（通称「黒本」）。

- 底本：第148次改正版（国空制第351号、令和7年11月20日改正）
- ファイル：`黒本148.pdf`

本資料は改正が頻繁であり、底本が更新された場合は全体を再構築する前提とする。
特定の改正版に依存した処理を実装してはならない。
総ページ数・ページ番号は改正により変化するため、仕様上の定数として扱わない。

### 1.2 利用者

航空管制業務に従事する者が各々で利用することを想定し、複数利用者の権限管理やアカウント機能は持たない。

### 1.3 利用シーン

| # | シーン | 必要な機能 |
|---|--------|-----------|
| S0 | 特定の規定をすばやく確認したい | 階層構造とそれに対応するUI |
| S1 | 特定の用語が含まれる箇所を探したい | 全文検索 |
| S2 | 定義や管制用語を確認したい | 定義や管制用語の一覧表示・検索 |
| S7 | 規定の名称に心当たりがあり、そこから辿りたい | 見出しの一覧・検索（4.5） |
| S3 | 図や表を含む規定を正確に確認したい | 図表の原寸表示、本文中の正しい位置への配置 |
| S4 | 後方乱気流方式を横断的に確認したい | 該当規定の一覧（4.8） |
| S5 | 参照元のpdfファイルを改定したい | 特定のページの差し替えや追加、ファイル全体の置換 |
| S6 | 移動中に手元で参照したい | スマートフォンでの閲覧 |

### 1.4 解決すること

- 大部の紙・PDFを線形に探す手間の解消
- 図表を含む規定を、本文と図表が分離しない形で読めること
- 端末を問わず同一の内容にアクセスできること

### 1.5 解決しないこと（対象外）

- 原文の編集・注釈の書き込み
- 複数改正版の差分表示
- 他の規程（第1〜第4、第6以降）への対応
- オフライン動作の保証
- 学習・出題機能 ※別途 `SPEC_QUIZ.md` にて定義予定

---

## 2. データモデル

### 2.1 基本モデル

文書全体を**順序付き木の集合**として表現する。

- すべての内容は木のノードである。本文・図・表・管制用語・注のいずれも例外ではない
- 各ノードは親を一つ持ち、兄弟間の順序を持つ
- 木の深さに上限を設けない
- 分類（ノード種別）は**ノードの属性**であって、ノードの代わりではない

原本の紙面上の位置（座標・ページ）は木を復元するための手がかりであり、
木そのものの構造を決定しない。**視覚的な近接は所属を意味しない。**

**根**

第1段（`Ⅰ` 〜 `Ⅶ`）のノードを根とする。文書全体を表す単一の根は設けない。
したがって木は第1段の数だけ存在し、`depth` は第1段を0として数える。

**データベースは原本から完全に再構築可能な派生物とする。** 利用者が作成した
情報（ブックマーク等）をデータベース内に保持してはならない。改正のたびに
全体を作り直せることを、常に成立する前提とする。

**索引の対象**

原本のうち、木として構造化する範囲を次のとおりとする。

| 範囲 | 木に含めるか | 根拠 |
|------|------------|------|
| 第1段 Ⅰ〜Ⅶ の本文 | 含める | 本仕様の主たる対象 |
| 目次 | **含めない** | 本文の見出しの複製であり、固有の内容を持たない |
| 改正履歴 | **含めない** | 規定の内容ではなく、特定の改正版に固有の情報である |
| 章の扉ページ | 未決（U8） | |
| Ⅴ章 | 当面対象外（U5） | |

別表は第1段 Ⅰ〜Ⅶ のいずれかに属するため、上表1行目に含まれる。

**目次を含めない理由**

- 内容が本文の見出しの複製であるため、検索において本文の一致と重複する
- ページラベルの書式が本文と異なり（2.11 の2成分形式・3成分形式の
  いずれにも該当しない）、構造の自己検証（2.14）が恒常的に
  不一致を報告することになる

**改正履歴を含めない理由**

改正履歴は規定そのものではなく、当該改正版の来歴を示す情報である。
本仕様は特定の改正版に依存しない設計を方針とする（1.1）ため、
版に固有の情報を木に取り込むことは方針と整合しない。

改正の適用範囲を扱う手順は 2.15 に定めるが、そこで用いるのは改正告示で
あり、原本に印刷された改正履歴ではない。

**別表を `search_text` から除外しない理由**

別表は検索の既定範囲（4.4.1）からは外れるが、`search_text` からは
除外しない。別表は本文から参照されるため、参照先がノードとして
存在しなければ当該参照は構造的に解決不能となり、未解決参照の件数
（3.14）が品質指標として機能しなくなる。

### 2.2 ノード種別

ノード種別は排他的であり、一つのノードは一つの種別を持つ。

| 種別 | 内容 | 原本での目印 |
|------|------|------|
| `SECTION` | 階層記号を持つ項目。自身の本文と子ノードを持つ | `(２)` `ａ` `ア` 等 |
| `TERM` | 用語定義。用語名と定義文の対 | なし |
| `PHRASE` | 管制用語。邦文と英文の対 | `★` |
| `EXAMPLE` | 管制用語群に対する用例。個々の `PHRASE` には従属しない（2.8） | `〔例〕` |
| `NOTE` | 注記。番号を持つ場合と持たない場合がある | `注` `注１` |
| `FIGURE` | 図 | なし |
| `TABLE` | 表 | なし |

「原本での目印」は種別を識別するための手がかりであり、`nodes.marker`
（3.2）とは異なる。`marker` に格納するのは個体を識別する番号であり、
`SECTION` の階層記号と `NOTE` の番号のみが該当する。`★` や `〔例〕` は
種別を示すだけなので `marker` には格納しない。

**種別に含めないもの**
- `【】` 見出し → `SECTION` の属性（2.5）
- 四角囲みのラベル → ノードに付与される属性（2.6）
- 波括弧 → 組版上の表現であり、内容の分類ではない（2.8）

#### 2.2.1 `TERM` の判定条件

`TERM` は、第1段 `Ⅰ` の直下にある**見出しが `定義` である節**に含まれる項目に限る。

- 用語名と定義文の対で構成される
- 用語名には括弧が付随し、括弧内に英語表記等を含む（3.9）

**`TERM` に含めないもの**
- 本文中に現れる略称の定義（`以下「TCA」という。` 等）。これは `SECTION` の
  本文の一部として扱う
- 定義の節の外にある「〜とは、〜をいう。」の形式の記述

定義の節の範囲は物理ページで指定してはならない（2.11）。木の構造上の位置に
より判定する。

### 2.3 階層記号と木の構築

原本は最大8段の入れ子を持つ。

| 段 | 記号 | `marker_type` | 例 |
|----|------|---------------|-----|
| 1 | ローマ数字 | `roman` | `Ⅲ` |
| 2 | 括弧付きローマ数字 | `paren_roman` | `(Ⅲ)` |
| 3 | 算用数字 | `number` | `３` |
| 4 | 括弧付き算用数字 | `paren_number` | `(２)` |
| 5 | 英小文字 | `alpha` | `ａ` |
| 6 | 括弧付き英小文字 | `paren_alpha` | `(a)` |
| 7 | カタカナ | `kana` | `ア` |
| 8 | 括弧付きカタカナ | `paren_kana` | `(ア)` |

**本書で「第N段」と記す場合、それは上表の `marker_type` を指す。**
木の中での深さ（`depth`）とは無関係である。段が省略されるため（2.3.1）、
例えば「第4段」（`paren_number`）のノードの `depth` は一定しない。

#### 2.3.1 段の省略

**任意の段が省略されうる。** 記号の順序は保たれるが、連続する必要はない。

実測される主な遷移：

| 遷移 | `marker_type` | 省略される段 |
|------|---------------|-------------|
| `(２)` → `(a)` | `paren_number` → `paren_alpha` | 第5段 |
| `(２)` → `ア` | `paren_number` → `kana` | 第5・6段 |
| `３` → `(a)` | `number` → `paren_alpha` | 第4・5段 |
| `３` → `ア` | `number` → `kana` | 第4〜6段 |

第5段 `ａ` は例外的で、第4段 `(２)` の直下に第6段 `(a)` が現れる形が標準である。

したがって **`marker_type` から木の深さを導出してはならない。** 深さは木の中での
実際の位置としてのみ定義される。`marker_type` と `depth` は独立に保持する。

**段の順序**

段は 2.3 の表の順に全順序をなす。省略があっても順序関係は保たれる。

```
roman < paren_roman < number < paren_number < alpha < paren_alpha < kana < paren_kana
```

親ノードの段は、常に子ノードの段より前である。この順序により、
「第4段以下」のような範囲指定が、段の省略にかかわらず判定できる。

段を持たないノード（`marker_type` が空、または `note`）は、この順序の
対象外である。範囲指定の判定には、直近の `SECTION` である祖先の段を用いる。

#### 2.3.2 親子の同一行併記

親ノードが固有の本文を持たない場合、その記号は**最初の子の記号と同一行に並記**される。

```
(１)ａ レーダー誘導(以下「誘導」という。)は、IFR機及び…
ｂ(a) 最低誘導高度は、地形、レーダー性能、飛行経路…
ア(ア) 空港監視レーダーのレーダーサイトから40海里未満…
```

`(１)ａ` は「`(１)` の下に `ａ`」であり、一つの記号ではない。この場合、親ノードは
本文を持たず、子ノードのみを持つ。

行頭から記号を一つだけ取り出す処理をしてはならない。連続する記号をすべて
取り出し、親子関係として展開する。

**非隣接段の併記**

実測される併記はいずれも隣接する段の組み合わせだが、段は省略されうるため
（2.3.1）、`(２)(a)` のように第5段を飛ばした併記が生じうる。また3つ以上の
記号が並記される可能性も否定できない。

どちらが親であるかは 2.3.1 の段の順序により判定する。並記された記号の
個数や隣接性を前提とした処理をしてはならない。

#### 2.3.3 字下げの扱い

**行頭のx座標を階層判定の根拠としてはならない。** 同一段の記号でも、2.3.2 の
併記が起きるか否かでx座標が変わる。字下げは補助的な手がかりにとどめる。

#### 2.3.4 第8段の用法

第8段 `(ア)` の出現は稀である。用法は複数条件の列挙であり、
他の段と異なる扱いを必要としない。

### 2.4 論理パス

各ノードは、根からの階層記号を連結した論理パスを持つ。

段は省略されうるため（2.3.1）、パスの長さと要素の種類は箇所により異なる。

```
Ⅲ/(Ⅳ)/4/(2)/(a)        第5段を省略した形（標準的）
Ⅲ/(Ⅳ)/4/(2)/b/(a)/ア    第5段を含む形
Ⅲ/(Ⅲ)/3/(2)/b          第4段までの形
```

**書式**
- 区切り文字は `/` とし、前後に空白を置かない
- 各要素には `marker_norm`（正規化した先頭記号、2.10）を用いる
- ローマ数字とカタカナは全角、英数字は半角となる（2.10）。
  したがって原本の `ｂ` は `b`、`(２)` は `(2)` となる一方、`ア` `Ⅲ` は全角のまま

**性質**
- 論理パスは文書内で一意である
- 論理パスは**紙面上の位置に依存しない**

**ただし論理パスは版をまたいで不変ではない。** 途中に新しい項目が挿入されると
後続の番号が繰り下がるため、同じ論理パスが別の内容を指すようになる。

論理パスは「現在の版における座標」であり、永続的な識別子ではない。
版をまたぐ参照には 2.13 のアンカーを用いる。

**階層記号を持たないノードのパス**

`FIGURE` `TABLE` `PHRASE` `EXAMPLE` `NOTE` は階層記号を持たない。これらの
論理パスは、親のパスに種別と兄弟内順序を付加して構成する。

```
Ⅲ/(Ⅲ)/3/(2)/b/(a)#table1
Ⅲ/(Ⅲ)/3/(2)/b/(a)#table2
```

**採番の規則**
- 種別ごとに独立して1から採番する。`FIGURE` が2件、`TABLE` が1件ある親の下では
  `#figure1` `#figure2` `#table1` となる
- 採番の順序は紙面上の出現順とする。`seq`（2.7）と一致する。
  再構築のたびに採番が変わってはならない

`EXAMPLE` は `PHRASE` の兄弟である（2.8）ため、同じ親のもとで採番される。

```
Ⅲ/(Ⅱ)/6/(1)/b/(e)#phrase1
Ⅲ/(Ⅱ)/6/(1)/b/(e)#phrase2
Ⅲ/(Ⅱ)/6/(1)/b/(e)#example1
Ⅲ/(Ⅱ)/6/(1)/b/(e)#example2
```

`NOTE` が `注１` のような番号を持つ場合も、その番号は階層記号ではないため
パスの構成には用いない。`#note1` のように機械的に採番する。

この付加部分は原本に由来しない機械的な採番であり、版をまたぐ安定性を持たない。

### 2.5 見出し

`【】` で囲まれた文字列は、階層の一段ではなく、**直後の `SECTION` の名前**である。

```
【最低誘導高度】
(２)ａ 誘導は、レーダー進入における最終進入中の航空機の誘導…
   ｂ(a) 最低誘導高度は、地形、レーダー性能、飛行経路…
```

上記は「`(２)` というノードが `最低誘導高度` という見出しを持つ」と解釈する。
`【】` を独立したノードにしてはならない。番号体系の親子関係が一段ずれるため。

**性質**
- 付与される対象は第4段（`paren_number`）のノード
- 一つのノードに一つまで。持たないノードも存在する
- **見出しは、そのノードを根とする部分木全体の名前として機能する。**
  上例の `ｂ(a)` も `最低誘導高度` に関する記述である
- 子孫が独自の見出しを持つことはない
- 文書内で一意ではない（`【適 用】` のような定型見出しが多数出現する）
- 原本には均等割り付けの空白が含まれる（`【適 用】`）。2.10 に従い正規化する

**要件**：見出しの一覧を閲覧手段として提供する（S0）。一意でないため、
一覧表示では所属する論理パスを併記しなければならない。

**ラベルとの共存**

一つのノードが見出しとラベル（2.6）の両方を持つことがある。両者は独立した
属性であり、排他的ではない。

原本における配置の順序は、**見出し → ラベル → 階層記号**で固定である。

```
【インターセクション・デパーチャー等の間隔】
┌──────────────────┐
│ 後方乱気流管制方式 │
└──────────────────┘
(７)ａ 先行機と後続機が同方向に離陸する場合は…
```

いずれか一方のみが存在する場合もある。

**祖先による解決**

第4段より深いノード、および `FIGURE` `TABLE` `PHRASE` `EXAMPLE` `NOTE` 等の
見出しを持たないノードについて、そのノードが属する見出しを示す必要がある場合は、
**祖先を辿って直近の第4段の見出しを用いる。**

**祖先に第4段が存在しない場合、見出しは無いものとして扱う。** 段の省略により
（2.3.1）、第3段の直下に第6段が現れる系列では第4段のノードが存在しない。
また第4段のノードが見出しを持たない場合もある。いずれの場合も見出しは空となる。
表示にあたっては見出しの欄を省略し、階層パスのみを示す。

この解決は閲覧・検索結果・各種一覧に共通する処理である（4.4.2、4.7）。

### 2.6 ラベル

四角形の枠線で囲まれた文字列は、そのノードに付与される**分類ラベル**である。

```
┌──────────────────┐
│ 後方乱気流管制方式 │
└──────────────────┘
ｂ 先行機と後続出発機の間には以下の間隔を設定するものとする。
```

**性質**
- 付与される対象は `SECTION` のノードに限る。段は問わない
- 一つのノードに複数ありうる
- **部分木全体に継承する。** ラベルが付いたノードの子孫はすべてその分類に属する
- ノード種別ではない。ラベルの有無はノードの種類を変えない

**要件**：ラベルが付与された箇所を一覧する導線を提供する（S4、4.8）。
汎用の絞り込み機能としてではなく、独立した入口として提供する。

**見出しとの共存**

ラベルが付与されたノードが、見出し（2.5）を持つことがある。両者は独立した
属性である。配置の順序は 2.5 に定める。

見出し（2.5）との違いは次のとおり。両者とも部分木に及ぶが、意味が異なる。

| | 見出し `【】` | ラベル（四角囲み） |
|---|---|---|
| 意味 | そのノードが何についてか | そのノードが特定の方式に関する規定であることの明示 |
| 付与先 | 第4段の `SECTION` | `SECTION` の任意の段 |
| 個数 | 1つまで | 複数可 |
| 原本での表現 | `【】` の文字 | 描画された矩形 |
| 共存 | 同一ノードが両方を持ちうる | 同左 |

**現状**

原本で用いられるラベルは `後方乱気流管制方式` のみである。後方乱気流管制方式は
文書内の複数箇所で個別に規定されており、それらが後方乱気流管制方式の規定である
ことを明示する目的で付与されている。

将来もラベルの種類が増えない可能性が高いが、モデル上は複数種類を扱えるものとする。

### 2.7 図表の所属

図・表は本文と並ぶ兄弟ノードとして木に配置する。

**所属の決定順序**

1. 本文中の明示的な参照（`次表` `((２)－４図)` 等）が指す先のノード
2. 参照が無い場合、紙面上で図表より前に出現した直近のノードと同じ親に属する
3. 上記いずれでも決まらない場合は、確定不能として記録する（推測しない）

**紙面上の位置を所属の根拠としてはならない。** 図表は組版の都合でページ下部に
まとめて配置されることがあり、直前の本文が所属先とは限らない。

**兄弟内の順序**

所属は参照により決めるが、**兄弟内の順序（`seq`）は紙面上の出現順による。**
上記の禁止事項は所属の決定に関する規定であり、順序の決定を禁じるものではない。

例：`(a)` の下に `ア` `イ` と表が2つある場合を考える。表は紙面上 `イ` より
下に印刷されるが、`(a)` の本文が「次表の」と参照しているため、所属は `(a)` で
あり、`ア` `イ` と並ぶ兄弟となる。順序は紙面上の出現順により、
`ア`=1、`イ`=2、表=3、表=4 となる。

**構築の順序**

所属の決定には参照の解決（2.12）が必要であり、参照の解決には図表がノードと
して存在している必要がある。次の順序で解決する。

1. 図表をノードとして生成する。この時点では所属は未確定でよい
2. 参照を解決する
3. 上記の決定順序により所属を確定する

**図表の識別**
- 図は原本の図番号（`(２)－４`）を持つ。参照解決の鍵となる
- 表は番号を持たない場合がある
- 視覚的に隣接して配置された複数の表は、別々の表として分離する。
  罫線の連続性ではなく、内容と参照関係で判断する

### 2.8 管制用語

`★` で始まる邦文と、それに対応する英文の対を一つのノードとする。

**構成要素**

| 要素 | 内容 |
|------|------|
| 邦文 | `★` に続く日本語 |
| 英文 | 対応する英語 |
| プレースホルダ | `〔...〕` で囲まれた可変部分 |
| 選択 | `／` による代替 |
| 省略可能部分 | `(...)` |
| 用例 | `〔例〕` に続く英文。`PHRASE` の兄弟ノードとする（後述） |

**波括弧について**

共通部分の繰り返しを避けるための**組版上の表現**であり、意味を持たない。
`／` による選択と等価である。同一の内容が、邦文では `／`、英文では波括弧で
書き分けられる例が存在する。

したがって、波括弧は展開してテキストとして保持しなければならない。
画像として保持してはならない（検索できなくなるため）。

```
展開前： (RUNWAY〔number〕) { LVP / LVP FOR DEPARTURE
                            or
                            LOW VISIBILITY PROCEDURE / ... } IN FORCE.

展開後： (RUNWAY〔number〕) LVP / LVP FOR DEPARTURE IN FORCE.
        (RUNWAY〔number〕) LOW VISIBILITY PROCEDURE / ... IN FORCE.
```

**用例**

`〔例〕` に続く英文を `EXAMPLE` ノードとする。**用例は英文のみである。**
一つの `〔例〕` に複数の用例文が並ぶ場合、**用例文ごとに1ノードとし**、
`seq` で順序を保つ。

- 分割は文末のピリオドを手がかりとする。本文書においてピリオドは文末以外に
  用いられないため、この判定は信頼できる
- 行の折り返しは結合する。折り返し行と後続の用例文は行頭のx座標が一致するため、
  x座標で区別してはならない
- 表示にあたっては、先頭のノードにのみ `〔例〕` を付す
- 用例文がピリオドで終わっていない場合は検証結果（3.13）に記録する

**用例の親**

`〔例〕` は、直前に並ぶ管制用語**群の全体**に対応する。特定の一つに対応する
とは限らない。

```
エ 既知の場合は航空機の高度に関する情報及び型式
  ★トラフィック、〔数値〕時の方向…                    PHRASE
  ★トラフィック、あなたの〔方位〕…                    PHRASE
  ★トラフィック、〔位置〕の付近に多数のターゲット      PHRASE
  〔例〕Traffic, 12 o'clock, 10 miles, southbound.     EXAMPLE
        （以下8文）
```

上例では9つの用例文が3つの管制用語の全体に対応しており、最後の用例文は
3つ目の管制用語に対応する。

したがって **`EXAMPLE` の親は `PHRASE` ではなく、`PHRASE` 群と同じ親とする。**
`EXAMPLE` は `PHRASE` の兄弟として `seq` により並ぶ。

```
エ
├─ PHRASE 1
├─ PHRASE 2
├─ PHRASE 3
├─ EXAMPLE 1
├─ …
└─ EXAMPLE 9
```

個々の用例文がどの管制用語に対応するかは判定しない。対応関係は
兄弟としての並びが示すにとどめる。

**ページ境界**

用例はページをまたぐことがある。ページ境界で分断してはならない。

後続ページに現れる継続部分には `〔例〕` の表記がないため、ページを単位として
処理すると、前ページからの継続であることを判定できない。ノードはページを
またいで一つとする（2.11）。

**保持する情報**
- 展開前の構造（前置部・選択肢群・後置部）— 原本の体裁を再現するため
- 展開後の全文の配列 — 検索の対象とするため
- プレースホルダの一覧 — 検索時に除外可能とするため

### 2.9 表

- セル構造を行列として保持する。結合セルは空文字で表現する
- 見出し行を持つ場合はそれを識別する。**見出し行は表の先頭から連続する行に
  限る。** 表の途中に現れる見出し行は扱わない `[未決 U9]`
- 罫線を持たない表が存在しうる `[未決 U2]`

### 2.10 テキストの保持と正規化

各ノードは二つのテキストを持つ。

| | 内容 | 用途 |
|---|------|------|
| 原文 | 原本の表記をそのまま保持 | 表示 |
| 正規化テキスト | 下記の規則を適用 | 検索 |

**正規化規則**
- 均等割り付けの空白を除去する（`【適 用】` → `【適用】`）
- 行の折り返しによる改行を除去し、一続きの文にする
- 段落の区切りとしての改行は保持する
- 全角英数字を半角に統一する
- ローマ数字（`Ⅰ` 〜 `Ⅶ`）とカタカナは全角のままとする。半角へ変換しない
- 階層記号と本文の間の空白を除去しない（記号の切り出しに必要なため）

**禁止事項**
- テキストを「1行目」と「残り」に分割してはならない。文が分断され、
  行をまたぐ語句が検索できなくなる

### 2.11 ページラベル

ページ下部の表記は単なる連番ではなく、**そのページが属するセクションと、
その中での順序を表す構造的な情報**である。

**書式**

| 形式 | 例 | 意味 |
|------|-----|------|
| 3成分 | `(Ⅳ)－2－3` | 範囲記号 `(Ⅳ)`（第2段）、節番号 `2`（第3段）、その範囲の3ページ目。第1段は省略 |
| 2成分 | `Ⅳ－3` | 範囲記号 `Ⅳ`（第1段）、その範囲の3ページ目 |

3成分形式では第1段が省略されるため、`(Ⅳ)` と `Ⅳ` は**別のものを指す**。
前者は第2段の記号、後者は第1段の記号である。

現在の原本では、3成分形式は第1段 `Ⅲ` の範囲でのみ使用され、`Ⅲ` 以外の第1段は
2成分形式を用いる。ただしこれは各章の分量に依存した結果であり、改正により
他の章が3成分形式に移行する可能性がある。書式ルール自体は不変とする。

**上記に該当しない書式**

目次のページには `－4－` のような、範囲記号・節番号を持たない書式が
用いられる。これは 2.1 により木の対象外であるため、本仕様の
ページラベルとして扱わない。構造の自己検証（2.14）の対象からも除く。

改正履歴のページについても同様に扱う。

**保持の方法**

生の文字列で保持してはならない。次の構成要素に分解して保持し、表示時に
書式を再構成する。

| 要素 | 内容 |
|------|------|
| 書式種別 | 3成分 / 2成分 |
| 範囲記号 | 第2段の記号（3成分）または第1段の記号（2成分） |
| 節番号 | 第3段の番号（3成分のみ） |
| 順序 | 当該範囲内での通し番号 |

**識別子としての使用を禁止する**

ページラベルは改正により容易に変動する。

| 改正の種類 | 順序 | 節番号 | 範囲記号 |
|---|---|---|---|
| 分量の増減 | **変動** | 不変 | 不変 |
| 節の末尾追加 | **変動** | 不変 | 不変 |
| 第2段の末尾追加 | **変動** | 不変 | 不変 |
| 節の途中挿入 | **変動** | **変動** | 不変 |

順序はあらゆる改正で変動する。ページラベルをノードの識別子、外部からの
参照先、または永続的なキーとして用いてはならない。

**用途**

1. 表示。原本を参照する際の共通言語として
2. 改正作業の入力。改正告示が指定する差し替え範囲の表現として
3. 構造の検証（2.14）

### 2.12 参照

本文中の参照は、ノード間のリンクとして保持する。

| 種別 | 例 | 解決先 |
|------|-----|--------|
| 図参照 | `((２)－４図)` | `FIGURE` ノード |
| 表参照 | `次表` | `TABLE` ノード |
| 条項参照 | `(Ⅳ)６(４)ｂ(b)ア` | `SECTION` ノード（論理パス） |
| 相対参照 | `前項` | 文脈依存 `[未決 U4]` |

解決できなかった参照は、解決先を空のまま記録する。破棄してはならない。
未解決の件数を品質指標として利用する。

**参照の境界**

接続表現は参照の境界である。`(２)ａ(a)及び(b)` は2つの参照であり、
`(２)ａ(a)` と `(b)` に分割する。連続する記号を機械的に一つの参照に
まとめてはならない。

境界となる接続表現：`及び` `又は` `若しくは` `並びに` `、`

**範囲指定**

`から` は並列ではなく**範囲**を表す。前後の参照を分割してはならない。

```
(a)から(c)に掲げる事項    → (a) (b) (c) の3ノードを指す一つの参照
```

- `から` の前後は同じ段の記号でなければならない
- 範囲の展開には親ノードの子の一覧が必要なため、木の構築後に解決する
- 展開結果が複数ノードとなるため、解決先は 3.11 の `ref_targets` に記録する

**参照先が子孫か否かの判定**

参照が明示する最も浅い段を S とする。

| 条件 | 解釈 |
|------|------|
| 参照元ノードのパスに段 S が**含まれない** | 参照元の子孫への参照 |
| 参照元ノードのパスに段 S が**含まれる** | 参照元の外への参照。省略された段を補完する |

子孫への参照では、参照元のパスをそのまま前置する。補完は行わない。

**理由**：兄弟または上位の階層を指す場合、参照元と異なる値を示す必要があるため、
参照元のパスに含まれる段が必ず明示される。子孫を指す場合はその必要がない。

**例**：p254 の同一ページ内に、記号が同じで解釈が異なる参照が存在する。

| 参照元のパス | 参照 | 最浅段 | 参照元に段Sが | 解決先 |
|--------------|------|--------|---------------|--------|
| `Ⅲ/(Ⅳ)/5/(3)/b` | `(a)から(c)` | 第6段 | 含まれない | `Ⅲ/(Ⅳ)/5/(3)/b/(a)`〜`(c)` |
| `Ⅲ/(Ⅳ)/5/(3)/a/(c)/イ` | `(a)若しくは(b)` | 第6段 | 含まれる | `Ⅲ/(Ⅳ)/5/(3)/a/(a)`、`(b)` |

前者は子への参照、後者は祖先の兄弟への参照である。記号のみでは区別できず、
参照元のパスとの照合により判別する。

**省略された段の補完**

参照が子孫への参照でない場合、明示されていない浅い段を補完する。
条項参照は根に近い段を省略する。ページラベル（2.11）と同じ性質である。

```
(Ⅳ)６(４)ｂ(b)ア        第1段が省略されている
```

省略された各段について、次の順に値を決定する。

1. **同一ノードの本文中で、当該参照より前に出現した参照を、出現順の逆順に
   走査する。その段が明示されている最初の参照が見つかれば、その値を用いる。**
2. いずれの先行参照にも明示されていない場合、参照元ノード自身のパスの
   同じ段の値を用いる。

**段ごとに独立して解決する。** ある参照が第3段を先行参照から引き継ぎ、
第1段・第2段を参照元から補うことがありうる。

**例1**：参照元が `Ⅲ/(Ⅳ)/3/(2)/c/(b)` で、本文が次の場合。

```
５(２)ｃ(b)又は(３)ｃ(b)により…
```

| 参照 | 第1段 | 第2段 | 第3段 | 解決先 |
|------|-------|-------|-------|--------|
| `５(２)ｃ(b)` | 参照元 `Ⅲ` | 参照元 `(Ⅳ)` | 明示 `5` | `Ⅲ/(Ⅳ)/5/(2)/c/(b)` |
| `(３)ｃ(b)` | 参照元 `Ⅲ` | 参照元 `(Ⅳ)` | 先行参照 `5` | `Ⅲ/(Ⅳ)/5/(3)/c/(b)` |

2つ目の参照に規則1を適用しない場合、第3段が参照元の `3` となり、誤った箇所に
解決される。

**例2**：先行参照を逆順に走査する場合。

```
５(２)ｃ(b)、(３)ｃ(b)、６(１)ａ又は(２)ｂ
```

| 参照 | 第3段 | 根拠 |
|------|-------|------|
| `５(２)ｃ(b)` | `5` | 明示 |
| `(３)ｃ(b)` | `5` | 直前の参照が明示 |
| `６(１)ａ` | `6` | 明示 |
| `(２)ｂ` | `6` | 逆順に走査し、直前の `６(１)ａ` が明示 |

最後の参照が引き継ぐのは直前の `6` であり、最初の `5` ではない。

**引き継ぎの範囲**

引き継ぎは、**同一の論理パスを持つノードの正規化テキスト内に限る。**

論理パスは文書内で一意である（2.4）ため、この範囲は一つのノードの本文と
一致する。別のノードの本文にある参照を引き継いではならない。

ノードの本文が複数の文からなる場合、句点をまたぐ引き継ぎも許容する。
原本において参照の省略が文の境界でリセットされるとは限らないため。

子孫への参照は補完を行わないため、引き継ぎの対象とならない。ただし後続の
参照が引き継ぐ値としては、明示された段のみが有効である。

**解決の順序**

1. 参照の境界を判定し、範囲指定を一つの参照としてまとめる
2. 参照が子孫への参照か否かを判定する
3. 子孫への参照でなければ、省略された段を補完する
4. 論理パスを構成し、木に照会して解決先を確定する
5. 解決先が存在しなければ未解決として記録する

**注記**
- 参照は本文中の出現順に処理しなければならない。各参照を独立に解決しては
  ならない
- 参照は正規化テキスト（2.10）から抽出する。原本では参照が行をまたいで
  折り返されることがあるため
- 参照解決は同一の第1段の中を探索する。第1段をまたぐ参照が存在する場合は
  解決できず、未解決として記録される
- 引き継ぎにより解決された参照は、誤りが連鎖しやすい。全件を検証の対象と
  する（2.14）

### 2.13 版をまたぐアンカー

改正を越えて同一箇所を指す必要がある参照（`EXAMPLES.md`、正解データ、
将来の利用者データ）には、番号に依存しないアンカーを用いる。

**アンカーの構成**

| 要素 | 性質 |
|------|------|
| 見出し | 移動しても名称が保たれやすい |
| ラベル | 分類は改正で変わりにくい。ただし現状1種類のみのため絞り込みの効果は小さい |
| 特徴語 | 本文中の特徴的な語句。文言変更で失われる |
| 論理パスの参考値 | 前版での位置。解決の手がかりとしてのみ用いる |

**各要素は単独では箇所を特定できない。** 見出しは一意でなく（2.5）、ラベルは
現状1種類しかない。複数の要素を組み合わせて絞り込むことを前提とする。

**要件**：改正後にアンカーを再解決し、解決済み・移動・失敗の三状態に
分類して報告できること。失敗したアンカーのみを人が確認する運用とする。

版をまたいで不変な識別子は原本に存在しない。本文の文言を手がかりとする以外に
方法はないという前提に立つ。

### 2.14 構造の自己検証

ページラベルは原本自身が宣言する所属情報であるため、構築した木の正しさを
検証する基準として利用できる。

**検証項目**

| 項目 | 不一致が示すもの |
|------|-----------------|
| ページ内のノードの所属と、ページラベルの範囲・節番号が一致するか | 階層の誤認識 |
| 範囲記号または節番号が変化したとき順序が1にリセットされ、それ以外のとき前ページ+1であるか | セクション境界の誤り |
| 節番号が連続しているか | 節の取りこぼし |
| ページラベルを取得できたか | ページの読み取り失敗 |
| ページをまたぐノードの件数 | 境界での誤った分断。件数が想定より少なければ分断を疑う |

**参照に関する検証**

参照解決は誤りが生じやすいため、次を記録する。子孫への参照は正当であり、
誤りとして扱わない（2.12）。

| 記録する事項 | 目的 |
|--------------|------|
| 引き継ぎにより段が補完された参照 | 誤りが連鎖しやすいため全件を確認対象とする |
| 未解決の参照 | 抽出または解決の失敗 |
| 解決先が参照元自身またはその祖先である参照 | 循環参照。抽出の誤りである可能性が高い |
| 範囲指定の展開結果が0件または1件である参照 | 範囲の解釈または抽出の誤り |
| `is_descendant` が1で解決先を持たない参照 | 子孫判定の誤り、または段の省略 |

**要件**：全ページについて上記を検査し、不一致を一覧として報告できること。
不一致を自動的に修正してはならない。報告するにとどめる。

この検証は改正後も有効である。番号が変動しても、ページラベルと木が整合すると
いう関係は常に成立するため。

### 2.15 改正への対応

**部分再構築**

改正告示は差し替え範囲をページラベルで指定する。該当する範囲の部分木を
破棄して再構築し、当該範囲内の順序を振り直す。他の範囲には影響しない。

**全体再構築**

次のいずれかに該当する場合は部分再構築を行わず、全体を作り直す。

- 差し替えにより新しい節番号が出現する（後続の番号が繰り下がるため）
- 新しい第2段が出現する
- 書式種別が変化する（2成分形式から3成分形式への移行等）

判断に迷う場合は全体再構築を選ぶ。データベースは派生物であり、
作り直すことに副作用がないため（2.1）。

**改正後の手順**
1. 全体または部分を再構築する
2. 2.14 の自己検証を実行し、不一致を確認する
3. 2.13 のアンカーを再解決し、失敗分を確認する

### 2.16 未決事項

| # | 内容 | 影響 |
|---|------|------|
| U1 | `注` の係り先の判定規則。紙面上の直前のノードとは限らず、字下げも決定的でない | 2.2 / 3.10 |
| U2 | 罫線を持たない表が存在するか。存在する場合の判定条件 | 2.9 |
| U3 | 枠線で囲まれた領域のうち、ラベルではなく複数行の本文を含むものが存在するか | 2.6 |
| U4 | `前項` 等の相対参照を解決するか、原文のまま保持するか | 2.12 |
| U5 | Ⅴ章は当面索引の対象外とし、構造の定義および格納方法は別途定める | 2.1 / 3章全体 / 4.12 |
| U7 | 図中に含まれる文字列を検索の対象とするか。含める場合、`search_text` に `field` を追加する | 2.7 / 3.12 |
| U8 | 章の扉ページ（`Ⅲ 管制方式基準` のみのページ）をノードとして扱うか。扱う場合の `node_type` | 2.1 / 3.2 |

U6（別表・目次・改正履歴の扱い）は 2.1 において解決した。
番号は再利用しない。

---

## 3. データベーススキーマ

### 3.1 方針

- 2章で定義したデータモデルを、そのままの構造で格納する
- データベースは原本から完全に再構築可能な派生物とする（2.1）。利用者が作成した
  情報を含めない
- 木構造は自己参照テーブルで表現し、深さの上限を設けない
- 内容の種別に共通する情報は `nodes` に、種別固有の情報は拡張テーブルに置く
- 型は `INTEGER` / `TEXT` / `REAL` の3種で表現する

以下の表で「必須」は NOT NULL を意味する。

---

### 3.2 nodes（ノード）

文書の木構造を保持する中心テーブル。すべての内容が1行に対応する。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー。再構築のたびに変化する。外部から参照禁止（2.4） |
| `parent_id` | INTEGER | | 親ノード。根のみ空 |
| `seq` | INTEGER | ✓ | 兄弟内での順序。1から連番 |
| `depth` | INTEGER | ✓ | 木の中での深さ。根を0とする |
| `node_type` | TEXT | ✓ | 2.2 の種別 |
| `path` | TEXT | ✓ | 論理パス（2.4）。文書内で一意 |
| `marker` | TEXT | | 項目の先頭記号の原本表記。`SECTION` は階層記号、`NOTE` は `注` または `注１` 等。他の種別は空 |
| `marker_type` | TEXT | | 記号種別。`SECTION` は 2.3 の8種、`NOTE` は `note`。`depth` とは独立 |
| `marker_norm` | TEXT | | 正規化した先頭記号 |
| `heading` | TEXT | | `【】` 見出しの原本表記（2.5） |
| `heading_norm` | TEXT | | 正規化した見出し |
| `text_raw` | TEXT | | 本文の原本表記（2.10） |
| `text_norm` | TEXT | | 正規化した本文（2.10） |
| `page_start` | INTEGER | ✓ | 開始物理ページ |
| `page_end` | INTEGER | ✓ | 終了物理ページ。またがらない場合は `page_start` と同値 |

**制約**
- `(parent_id, seq)` は一意
- `path` は一意
- `parent_id` は同一テーブルの `id` を参照する
- `page_start` <= `page_end`
- `parent_id` が空のノードは第1段のみ（2.1）。`depth` は 0 とする
- `heading` が非空となるのは第4段のノードのみ（2.5）
- `marker_type` が `note` のノードは論理パスの構成に参加しない（2.4）

**注記**
- `id` は再構築のたびに変化する。外部ファイル・利用者データ・報告書等から
  `id` を参照してはならない。位置を指すには `path`（2.4）を用いる
- `marker_type` から `depth` を導出してはならない（2.3.1）
- `text_raw` を「1行目」と「残り」に分割するカラムを設けてはならない（2.10）
- 親が固有の本文を持たない場合（2.3.2）、`text_raw` は空となる

---

### 3.3 pages（ページ）

物理ページと、ページ下部の表記（2.11）を保持する。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `physical_page` | INTEGER | ✓ | 主キー。PDFの通し番号 |
| `label_raw` | TEXT | | ページ下部表記の原本表記。表示・検証用 |
| `label_format` | TEXT | | `three_part` / `two_part` |
| `scope_marker` | TEXT | | 範囲記号。3成分なら第2段、2成分なら第1段の記号 |
| `section_number` | INTEGER | | 節番号。3成分形式のみ |
| `ordinal` | INTEGER | | 当該範囲内での順序 |
| `scope_node_id` | INTEGER | | ページ下部表記が示すセクションのノード |

**制約**
- `label_format` が `three_part` のとき `section_number` は必須
- `label_format` が `two_part` のとき `section_number` は `0` とする。
  空にしてはならない。空の場合、一意制約が機能しなくなるため
- `(label_format, scope_marker, section_number, ordinal)` は一意

**注記**
- ページ下部表記を識別子・外部参照・永続キーとして用いてはならない（2.11）
- 表記を取得できないページ（白紙、章の扉）が存在する。この場合ラベル関連の
  カラムは空とする。一意制約の対象外となる
- `scope_node_id` は 3.13 の検証で使用する

---

### 3.4 labels（ラベル）

四角囲みのラベル（2.6）。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー |
| `node_id` | INTEGER | ✓ | ラベルが付与されたノード |
| `text` | TEXT | ✓ | ラベルの文字列 |
| `text_norm` | TEXT | ✓ | 正規化した文字列 |
| `physical_page` | INTEGER | ✓ | 矩形が描画されたページ |
| `x0` `y0` `x1` `y1` | REAL | ✓ | 矩形の座標。検証用 |

**制約**
- `node_id` が指すノードの `node_type` は `SECTION` でなければならない（2.6）

**注記**
- ラベルの効力は `node_id` を根とする部分木全体に及ぶ（2.6）。継承関係を
  テーブルとして展開せず、問い合わせ時に木を辿って解決する
- `physical_page` は矩形が描画された位置であり、付与先ノードの
  `nodes.page_start` と一致するとは限らない
- 現状 `text` は `後方乱気流管制方式` のみだが、複数種類を扱えるものとする

---

### 3.5 figures（図）

`node_type` が `FIGURE` のノードの固有情報。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `node_id` | INTEGER | ✓ | 主キー。`nodes` を参照 |
| `number` | TEXT | | 原本の図番号（`(２)－４`）。参照解決の鍵 |
| `number_norm` | TEXT | | 正規化した図番号 |
| `image_path` | TEXT | ✓ | 画像ファイルの相対パス |
| `physical_page` | INTEGER | ✓ | 図が描画されたページ |
| `x0` `y0` `x1` `y1` | REAL | ✓ | ページ内の座標 |

**制約**
- `physical_page` は `nodes.page_start` および `nodes.page_end` と一致する。
  図は複数ページにまたがらない

**注記**
- 画像はファイルとして保持し、本テーブルはパスのみを持つ
- 座標は必須とする。検出精度の検証、画像の再生成、将来の実行時描画への
  移行のいずれにも必要となるため
- `image_path` は座標から決定的に生成する。再構築のたびにファイル名が
  変わってはならない

---

### 3.6 tables（表）

`node_type` が `TABLE` のノードの固有情報。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `node_id` | INTEGER | ✓ | 主キー |
| `cells` | TEXT | ✓ | セル構造。行の配列の配列をJSONで保持 |
| `header_rows` | INTEGER | ✓ | 先頭から連続する見出し行の数。無い場合は0（2.9） |
| `physical_page` | INTEGER | ✓ | 表が描画されたページ |
| `x0` `y0` `x1` `y1` | REAL | ✓ | ページ内の座標 |

**制約**
- `physical_page` は `nodes.page_start` と一致する

**注記**
- 結合セルは空文字で表現する
- 視覚的に隣接する複数の表は、別々の行として保持する（2.7）

---

### 3.7 phrases（管制用語）

`node_type` が `PHRASE` のノードの固有情報。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `node_id` | INTEGER | ✓ | 主キー |
| `ja_raw` | TEXT | ✓ | 邦文の原本表記 |
| `en_raw` | TEXT | ✓ | 英文の原本表記 |
| `ja_structure` | TEXT | | 波括弧の構造をJSONで保持。無い場合は空 |
| `en_structure` | TEXT | | 同上 |
| `placeholders` | TEXT | | `〔〕` の一覧をJSONで保持 |

`ja_structure` / `en_structure` の形式（2.8）：

```json
{ "prefix": "(RUNWAY〔number〕)",
  "options": ["LVP / LVP FOR DEPARTURE",
              "LOW VISIBILITY PROCEDURE / LOW VISIBILITY PROCEDURE FOR DEPARTURE"],
  "suffix": "IN FORCE." }
```

**注記**
- 波括弧を画像として保持してはならない（2.8）
- 構造は原本の体裁を再現するために保持する。検索には 3.8 を用いる
- 用例は本テーブルに持たない。`EXAMPLE` ノードとして `PHRASE` の兄弟に
  配置される（2.8）。本テーブルと `EXAMPLE` の間に参照関係はない

---

### 3.8 phrase_expansions（管制用語の展開）

波括弧を展開した全文（2.8）。検索の対象とするため行に展開する。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー |
| `node_id` | INTEGER | ✓ | `phrases` を参照 |
| `lang` | TEXT | ✓ | `ja` / `en` |
| `variant` | INTEGER | ✓ | 選択肢の番号。1から連番 |
| `text_norm` | TEXT | ✓ | 展開後の全文（正規化済み） |
| `text_stripped` | TEXT | ✓ | `〔〕` を除去した全文 |

**制約**
- `(node_id, lang, variant)` は一意

**注記**
- 波括弧を持たない用語も、選択肢1件として本テーブルに登録する。
  検索処理を一本化するため

---

### 3.9 terms（用語定義）

`node_type` が `TERM` のノードの固有情報。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `node_id` | INTEGER | ✓ | 主キー |
| `term` | TEXT | ✓ | 用語名。括弧の前の部分 |
| `term_norm` | TEXT | ✓ | 正規化した用語名 |
| `paren_raw` | TEXT | | 括弧内の原本表記 |
| `term_en` | TEXT | | 括弧内から抽出した英語表記 |
| `abbrev` | TEXT | | `以下「〜」という。` で定義される略称 |

**用語名の構成**

用語名には括弧が付随する。括弧内の内容は一様ではない。

| 形態 | 例 |
|---|---|
| 英訳のみ | `アーク(Arc)` |
| 英訳と略称定義 | `移動開始時刻(Estimated off-block time。以下「EOBT」という。)` |
| 略語の展開 | `RVSM(Reduced vertical separation minimum)` |

最後の形態では、用語名が略語であり括弧内が展開形となる。他の形態と向きが逆である。

**注記**
- 定義文は `nodes.text_raw` / `nodes.text_norm` に格納し、本テーブルには持たない
- `paren_raw` は原本表記をそのまま保持する。抽出規則を変更した際に、
  原本へ戻らず再処理できるようにするため
- `term_en` と `abbrev` は空を許容する。抽出できない場合に値を作ってはならない。
  空の件数を品質指標として監視する
- 閲覧時の表示は `SECTION` と同等でよい。種別を分けるのは一覧・検索機能に利用するため(S2)

---

### 3.10 notes（注）

`node_type` が `NOTE` のノードの固有情報。係り先の判定は未決（U1）。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `node_id` | INTEGER | ✓ | 主キー |
| `attached_to` | INTEGER | | 係り先と推定したノード |
| `confidence` | TEXT | ✓ | `high` / `low` / `unresolved` |
| `indent_x` | REAL | ✓ | 行頭のx座標。判定の根拠として保持 |

**制約**
- `confidence` が `unresolved` のとき `attached_to` は空とする
- `confidence` が `high` または `low` のとき `attached_to` は必須

**注記**
- 判定規則が未確定のため、根拠となる情報を保持し、後から再判定できるようにする
- `confidence` が `high` 以外のものを人が確認する運用とする

---

### 3.11 refs（参照）

本文中の参照（2.12）。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー |
| `from_node_id` | INTEGER | ✓ | 参照元のノード |
| `seq_in_node` | INTEGER | ✓ | ノード内での出現順。1から連番 |
| `ref_type` | TEXT | ✓ | `figure` / `table` / `section` / `relative` |
| `ref_text` | TEXT | ✓ | 原本での参照表記 |
| `is_range` | INTEGER | ✓ | 範囲指定（`から`）なら1、そうでなければ0 |
| `is_descendant` | INTEGER | ✓ | 参照元の子孫への参照なら1（2.12） |
| `inherited_from` | INTEGER | | 段を引き継いだ先行参照の `id`。引き継ぎが無ければ空 |
| `resolution_note` | TEXT | | 補完した段とその根拠 |

**制約**
- `(from_node_id, seq_in_node)` は一意
- `inherited_from` が指す参照の `from_node_id` は、自身の `from_node_id` と
  一致しなければならない（2.12。引き継ぎは同一ノード内に限る）
- `is_descendant` が1のとき `inherited_from` は空。子孫への参照は補完を
  行わないため

**注記**
- 解決できなかった参照も行として記録する。破棄してはならない（2.12）
- 解決先は 3.11.1 の `ref_targets` に記録する。一つの参照が複数のノードを
  指すことがあるため（範囲指定）
- 解決先を持たない参照の件数を品質指標とする
- `resolution_note` には補完の根拠を人が読める形で記録する。
  例：`第3段=5(参照#12より引き継ぎ), 第1-2段=参照元より補完`
- 参照は `seq_in_node` の順に解決しなければならない（2.12）

#### 3.11.1 ref_targets（参照の解決先）

一つの参照が指す解決先。範囲指定（2.12）では複数行となる。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー |
| `ref_id` | INTEGER | ✓ | `refs` を参照 |
| `to_node_id` | INTEGER | ✓ | 解決先のノード |
| `seq` | INTEGER | ✓ | 範囲内での順序。1から連番 |

**制約**
- `(ref_id, seq)` は一意
- `refs.is_range` が0のとき、対応する行は1件以下

**注記**
- 未解決の参照は本テーブルに行を持たない

---

### 3.12 search_text（検索対象）

検索を単一のテーブルへの走査で完結させるための派生テーブル。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー |
| `node_id` | INTEGER | ✓ | 対象ノード |
| `field` | TEXT | ✓ | 下表のいずれか |
| `text` | TEXT | ✓ | 正規化済みのテキスト |
| `table_row` | INTEGER | | 表のセルの場合、行番号 |
| `table_col` | INTEGER | | 表のセルの場合、列番号 |

**`field` の種別**

| 値 | 由来 |
|---|---|
| `heading` | `nodes.heading_norm` |
| `body` | `nodes.text_norm` |
| `term` | `terms.term_norm` |
| `term_en` | `terms.term_en` |
| `phrase_ja` | `phrase_expansions.text_norm`（`lang='ja'`） |
| `phrase_en` | `phrase_expansions.text_norm`（`lang='en'`） |
| `table_cell` | `tables.cells` の各セル |

**要件**
- **1文字以上の任意の部分文字列で検索できること**
- 検索結果の順位は `field` と出現回数に基づいて算出する。
  順位付けの規則は 4章（画面と機能）に定める
- 転置索引・全文検索拡張を用いない。本文の総量は索引を要する規模ではなく、
  走査で十分な速度が得られるため

**表のセルの扱い**
- 表は**セル単位**で登録する。表全体を1行にまとめてはならない。
  無関係な語が同一行に同居し、順位付けが崩れるため
- `table_row` / `table_col` を保持し、一致したセルを表示時に特定できるようにする
- 一つの表から多数の行が生成される。検索結果を表単位にまとめる処理は
  4章に定める

**ラベルを対象としない**

ラベル（2.6）は `search_text` に登録しない。ラベルの文言は本文でも見出しでも
なく、検索結果として本質的でないため。同一の文言が多数のノードに付与されて
おり、これを検索対象とすると本文の一致が埋もれる。

ラベルが付与された箇所へは専用の導線（4.8）から到達する。

**既定の検索範囲との関係**

本テーブルには、木に含まれるすべてのノードを登録する。第1段による
絞り込みは行わない。

UI の既定の検索範囲（4.4.1）は第1段 `Ⅰ`〜`Ⅲ` に限定されるが、
これは表示側の判断であり、本テーブルの内容とは独立である。
既定の範囲を変更する際にデータベースの再構築を要しない設計とする。

**注記**
- 本テーブルは他テーブルの内容を複製する。再構築時に必ず作り直す
- 複製によりデータベースの容量は約2倍になる。許容する
- `node_id` は一致箇所そのものが属するノードを指す。`table_cell` なら
  `TABLE` ノード、`phrase_ja` なら `PHRASE` ノードである。
  検索結果に見出しを表示する際は、2.5 の祖先による解決を用いる

---

### 3.13 validation_issues（検証結果）

構造の自己検証（2.14）の結果。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | INTEGER | ✓ | 主キー |
| `kind` | TEXT | ✓ | 検証項目の種別 |
| `severity` | TEXT | ✓ | `error` / `warning` / `info` |
| `physical_page` | INTEGER | | 該当ページ |
| `node_id` | INTEGER | | 該当ノード |
| `message` | TEXT | ✓ | 内容 |

**注記**
- 検証で検出した不一致を自動的に修正してはならない（2.14）
- 本テーブルが空でないことは、構築の失敗を意味しない。人が確認する対象を示す

---

### 3.14 build_info（構築情報）

再現性の確保のため、構築時の情報を保持する。

| カラム | 型 | 必須 | 説明 |
|---|---|---|---|
| `key` | TEXT | ✓ | 主キー |
| `value` | TEXT | ✓ | 値 |

**保持する項目**

| キー | 内容 |
|---|---|
| `source_file` | 底本のファイル名 |
| `source_hash` | 底本のハッシュ値 |
| `revision` | 改正次数 |
| `built_at` | 構築日時 |
| `parser_version` | パーサーのバージョン |
| `node_count` | ノード総数 |
| `search_text_rows` | `search_text` の行数 |
| `multipage_node_count` | ページをまたぐノードの件数 |
| `unresolved_ref_count` | 未解決参照の件数 |

---

### 3.15 データベースに含めないもの

| 対象 | 理由・保管場所 |
|---|---|
| 利用者のブックマーク等 | データベースは再構築される派生物である（2.1） |
| 版をまたぐアンカー（2.13） | 外部ファイル（`EXAMPLES.md` 等）で管理し、解決結果は報告として出力する |
| 図・表の画像 | ファイルとして保持し、パスのみを持つ（3.5） |
| 元のPDF | 別途配置する |
| 全文検索の転置索引 | 3.12 の方針により作成しない |

---

### 3.16 参照整合性

| 参照元 | 参照先 |
|---|---|
| `nodes.parent_id` | `nodes.id` |
| `pages.scope_node_id` | `nodes.id` |
| `labels.node_id` | `nodes.id` |
| `figures.node_id` `tables.node_id` `phrases.node_id` `terms.node_id` `notes.node_id` | `nodes.id` |
| `phrase_expansions.node_id` | `phrases.node_id` |
| `refs.from_node_id` `notes.attached_to` | `nodes.id` |
| `refs.inherited_from` | `refs.id` |
| `ref_targets.ref_id` | `refs.id` |
| `ref_targets.to_node_id` | `nodes.id` |
| `search_text.node_id` | `nodes.id` |
| `validation_issues.node_id` | `nodes.id` |

拡張テーブル（`figures` `tables` `phrases` `terms` `notes`）の `node_id` は、
対応する `nodes.node_type` と一致していなければならない。

---

### 3.17 未決事項

| # | 内容 | 影響 |
|---|------|------|
| U9 | 表の途中に見出し行が現れる表が存在するか。存在する場合の扱い | 2.9 / 3.6 |
| U10 | 括弧内から `term_en` と `abbrev` を分離する規則。`RVSM` のように用語名と展開形の向きが逆になる場合を含む | 3.9 |

Ⅴ章の扱い（U5）および章の扉ページの扱い（U8）は 2.16 に記載する。

---

## 4. 画面と機能

### 4.1 方針

本章は**要件**を定める。何ができるべきかを規定し、画面の構成や配置の細部は
実装時に決定する。

- 利用者は原本の構造をある程度理解していることを前提とする。
  「Ⅲ管制方式基準の(Ⅳ)レーダー使用基準の４のどこかにある」という
  見当をつけて辿る使い方を主とする
- 2章・3章と異なり、本章の内容は使用しながら変更されることを前提とする。
  細部を固定しない

---

### 4.2 画面の構成

| 画面 | 役割 |
|------|------|
| 閲覧 | 階層を辿って本文を読む。主たる画面 |
| 検索 | 語句を入力して該当箇所を探す |
| 見出し一覧 | `【】` 見出しから探す |
| 用語定義一覧 | 用語定義から探す |
| 管制用語一覧 | 管制用語から探す |
| 後方乱気流管制方式 | 該当規定を一覧する（4.8） |
| 原本表示 | PDFの該当ページを表示する（4.9） |
| 診断 | 構築時の検証結果を表示する（4.10）。暫定 |

---

### 4.3 閲覧

#### 4.3.1 展開の規則

階層の深さによって、一度に表示する範囲を変える。

| 対象 | 表示 |
|------|------|
| 第1〜3段 | 直下の子の**表題**のみ。本文は表示しない |
| 第4段（`paren_number`）以下 | 選択したノードの**部分木をすべて展開**して表示 |

ここでいう表題とは、次を連結したものである。2.5 の `【】` 見出しとは別の概念で
あることに注意する。第2段・第3段は `【】` 見出しを持たない。

- 階層記号（`marker`）
- `【】` 見出し。持つ場合のみ
- 本文の冒頭。見出しを持たない場合の手がかりとして

第4段を開いた時点で、その項目に関する記述が一画面で読み通せる状態になる。
第1〜3段では選択肢を絞り込む段階であり、本文は表示しない。

**この規則は変更されうる。** 実際の分量によって第4段の部分木が長すぎる場合は、
折りたたみを導入する。

#### 4.3.2 表示する情報

各ノードについて、次を表示する。

| 要素 | 表示 |
|------|------|
| 階層記号 | 原本表記（`marker`） |
| 見出し | `【】` を含む原本表記。見出しを持つノード（第4段）に表示する |
| 本文 | 原文（`text_raw`） |
| ラベル | 四角囲みとして再現する |

- 見出しは部分木全体の名前として機能する（2.5）。子孫のノードには見出しを
  繰り返さない。閲覧中の位置がどの見出しに属するかは、現在位置（4.3.3）で
  確認できる
- 親子が同一行に併記される場合（2.3.2）、原本と同じ形で表示する
- `TERM` は `SECTION` と同等の見た目で表示する
- 段が省略されている場合（2.3.1）、字下げは木の深さではなく
  原本の見え方に従う

#### 4.3.3 現在位置

閲覧中は、根からの階層パスを常に表示する。

```
Ⅲ 管制方式基準 › (Ⅳ) レーダー使用基準 › ４ レーダー識別 › 【適 用】(１)
```

各要素は押下でき、その階層へ移動できる。

#### 4.3.4 参照の解決

本文中の参照（3.11）のうち、解決済みのものは押下で移動できる。
未解決の参照は原文のまま表示し、押下できないことが分かる見た目とする。

---

### 4.4 検索

#### 4.4.1 入力

- **1文字以上**の任意の文字列を受け付ける（3.12）
- 対象範囲を絞り込めること

**既定の検索範囲**

既定では、第1段が `Ⅰ` `Ⅱ` `Ⅲ` のいずれかである根を持つ部分木のみを
対象とする。第1段 `Ⅳ` 以降は既定の対象から除く。

除外の判定は**第1段の記号**により行う。ページ番号・物理ページを
判定に用いてはならない（2.11、1.1）。

第1段 `Ⅳ` 以降は表を主体とする資料であり、語句の一致した箇所のみを
断片として示しても内容を判断できない。これらを既定の対象に含めると、
本文の一致が埋もれる。

`Ⅳ` 以降も `search_text` には登録されている（3.12）。利用者が明示的に
範囲を広げた場合は対象とする。

| 範囲 | 対象 |
|------|------|
| 既定 | 第1段 `Ⅰ`〜`Ⅲ` のすべての `field`（3.12） |
| 全体 | 第1段を限定せず、すべての `field` |
| 見出しのみ | `heading`（第1段 `Ⅰ`〜`Ⅲ`） |
| 用語定義のみ | `term` `term_en` |
| 管制用語のみ | `phrase_ja` `phrase_en` |

「見出しのみ」「用語定義のみ」「管制用語のみ」は並列の選択肢として
提供する。いずれも目的が明確な場合に結果を大幅に減らせるため、
順位を下げるのではなく候補から除外する形とする。

見出しのみの検索は、階層を辿って目的の箇所に到達する方法（4.3）に代わる
手段である（S7）。規定の名称に心当たりがある場合、階層を降りるより直接的に
到達できる。

#### 4.4.2 結果の表示

各件について、次を表示する。

| 要素 | 内容 |
|------|------|
| 階層パス | 根からのパス。どこにあるかを示す |
| 見出し | 属する第4段の `【】` 見出し。2.5 の祖先による解決を用いる。存在しない場合は空 |
| 前後の文脈 | 一致箇所を含む前後のテキスト |
| 一致箇所 | 文脈内で強調表示する |

同一の見出しが文書内に多数存在するため（`【適 用】` 等の定型見出し）、
**階層パスの併記を必須とする。** 見出しのみでは区別できない。

一致が `table_cell` `phrase_ja` `phrase_en` の場合、対象ノード自身は見出しを
持たない。この場合も祖先を辿って直近の第4段の見出しを表示する（2.5）。

#### 4.4.3 順位付け

`field` と出現回数に基づいて算出する。優先順位は次のとおり。

1. `heading` — 見出しに一致
2. `term` `term_en` — 用語名に一致
3. `body` `phrase_ja` `phrase_en` — 本文に一致
4. `table_cell` — 表のセルに一致

同一 `field` 内では出現回数の多いものを上位とする。

**この規則は使用しながら調整する。** 初期値として定めるものであり、
仕様として固定しない。

#### 4.4.4 表の一致

表のセルは `search_text` に**セル単位**で登録されている（3.12）。
検索結果の表示にあたっては、同一の表に属する一致を**1件にまとめる**。
一つの表から多数の結果が並ぶことを避けるため。

まとめた結果、一つの表に複数の一致セルが含まれることがある。表を表示する際は
`table_row` / `table_col`（3.12）により、**該当するすべてのセルを強調する。**

---

### 4.5 見出し一覧

`【】` 見出しを一覧し、そこから該当箇所へ移動する。

見出しは規定の名称であり、階層を辿る閲覧（4.3）に代わる到達手段として
機能する（S7）。目的の規定の名称に心当たりがある場合、章・節を降りるより
直接的である。

- 階層順に並べる
- **各見出しに所属する階層パスを併記する。** 見出しは一意でないため（2.5）
- 語句による絞り込みを行えること

---

### 4.6 用語定義一覧

用語定義を一覧する。

- 用語名（`term`）の五十音順で並べる。英字で始まる用語（`RVSM` 等）は
  末尾にまとめ、英字順で並べる
- 英語表記（`term_en`）の英字順でも並べ替えられること
- 語句による絞り込みを行えること
- 用語名・英語表記・定義文のいずれからも探せること

---

### 4.7 管制用語一覧

管制用語を一覧し、そこから該当箇所へ移動する。

**表示する情報**

| 要素 | 内容 |
|------|------|
| 管制用語 | 邦文・英文（`PHRASE`） |
| 兄弟の管制用語 | 同じ親を持つ他の `PHRASE` |
| 用例 | 同じ親を持つ `EXAMPLE` のすべて |
| 親セクション | 管制用語が属するノードの階層記号と本文 |
| 見出し | 親セクションが第4段でない場合、直近の第4段の `【】` 見出し（2.5）。存在しない場合は空 |

管制用語は単独では適用条件が分からないため、どの規定に属するかを併記する。
親セクションが第4段より深い場合、そのノードだけでは文脈が不足するため、
祖先を辿って直近の第4段の見出しを表示する。

**兄弟の表示**

特定の管制用語を選択した場合、**同じ親を持つ管制用語と用例をすべて表示する。**

用例は管制用語群の全体に対応し、個々の用語には紐づかない（2.8）。選択した
用語のみを表示すると、対応する用例が判別できない。また同じ親を持つ管制用語は
状況に応じた選択肢の関係にあることが多く、併せて示すことが理解を助ける。

選択された管制用語は、他の兄弟と区別できる形で示す。

**要件**
- 階層順に並べる
- 語句による絞り込みを行えること
- 邦文・英文のいずれからも探せること
- 波括弧を持つ用語は、原本の体裁で表示する（2.8）

---

### 4.8 後方乱気流管制方式

後方乱気流管制方式に関する規定は文書内の複数箇所に分散している（2.6）。
これらを一覧する**専用の導線**を提供する。

- 汎用のラベル絞り込み機能としてではなく、独立した入口として提供する
- **一覧に並べるのはラベルが直接付与されたノードのみとする。** 継承先の子孫を
  個別の項目として並べてはならない。件数が過大になり一覧の意味を失うため
- 一覧は階層順に並べ、各件について階層パス・見出し・本文の冒頭を表示する
- 押下で移動した先では、当該ノードの部分木全体を表示する

実装にはラベル機構（3.4）を用いる。

---

### 4.9 原本表示

PDFの該当ページを表示する。

- **常時表示しない。** 利用者が要求したときのみ読み込む
- PDFは大きいため、閲覧・検索の動作を妨げてはならない
- 開始ページは閲覧中のノードの `page_start` とする
- ノードが複数ページにまたがる場合（`page_start` < `page_end`）、
  その範囲を利用者に示し、範囲内を移動できること

---

### 4.10 診断

構築時の検証結果（3.13）を表示する。

- 検証項目の種別・重大度で絞り込めること
- 該当するページまたはノードへ移動できること

**本画面は暫定である。** 構造の精度が安定した段階で削除する。

---

### 4.11 携帯端末での利用（S6）

- 閲覧・検索は携帯端末で支障なく利用できること
- 初回の読み込みで取得するデータを必要最小限とする。
  PDF（4.9）および図の画像は、必要になった時点で取得する
- 表は横幅が画面に収まらない場合がある。横方向にたどれること

---

### 4.12 対象外

| 項目 | 理由 |
|------|------|
| 原文の編集・注釈の書き込み | 1.5 |
| 利用者データの保存（ブックマーク等） | データベースは再構築される（2.1）。実装する場合は別の保管手段を要する |
| 複数改正版の差分表示 | 1.5 |
| Ⅴ章の閲覧 | U5により対象外 |
| 目次の閲覧・検索 | 木の対象外（2.1） |
| 改正履歴の閲覧・検索 | 木の対象外（2.1） |

---

### 4.13 未決事項

| # | 内容 | 影響 |
|---|------|------|
| U11 | 第4段の部分木が長大な場合の折りたたみ規則。実装後に分量を測り、必要なら対応する | 4.3.1 |
| U12 | 検索結果の前後の文脈をどの範囲で切り出すか | 4.4.2 |
| U13 | 検索の順位付けの重み。使用しながら調整する | 4.4.3 |
| U14 | 図をその場で展開する際、携帯端末での横幅をどう扱うか | 4.11 |
