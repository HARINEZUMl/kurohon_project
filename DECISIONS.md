# 設計判断の記録

> **このファイルは人間の意思決定者向けの記録である。**
> 実装作業にあたって参照する必要はない。実装上の制約は `CLAUDE.md`、
> 仕様は `SPEC.md` にある。
>
> 本ファイルには、破棄された旧実装に関する記述が含まれる。これらは判断の
> 経緯を残すためのものであり、現在の実装が従うべき事項ではない。

このファイルは、`SPEC.md` の各規定が**なぜそう決まったか**を記録する。
仕様そのものは `SPEC.md` に、実装上の注意は `CLAUDE.md` にある。

追記のみを行い、過去の記録は書き換えない。判断が覆った場合は、新しい記録を
追加して古い記録から参照する。

記録には、判断の根拠となった実測値を含める。`SPEC.md` は改正で陳腐化する
数値を持たない方針のため、それらはここに置く。

---

## D001. 旧実装を破棄し、一から再設計する

**日付**: 2026-08

**背景**

旧実装のデータベースを調査したところ、以下の構造的な問題が判明した。

- 図表のテーブルが存在しない。182の画像ファイルに対しレコードは0件。
  図表は本文テキスト中に `⬟ファイル名|幅|高さ⬡` というマーカーとして
  埋め込まれており、ID・図番号・キャプション・所属・座標のいずれも持てない
- 本文中の図参照122箇所に対し、解決された参照は0件
- `kind` カラムが階層（`H1`〜`H5`）と内容種別（`BODY` `DEF` `BOX_TITLE`）を
  混在させていた
- 階層が `parent_h1`〜`parent_h3` の3カラム固定で、原本の8段を表現できない。
  第6段以下は本文中に平文で埋没
- 親を文字列でコピーしており、`(Ⅰ)総則` が第1段 `Ⅱ` と `Ⅲ` の両方に存在する
  ため一意にならない
- `title` が本文の1行目、`text` がその続きという分割で、文が途中で分断されて
  いた。FTS5で別カラムのため、行をまたぐ語句が検索できない
- 階層パスが113通りしかなく、用語定義215件がほぼ1つの箱に入っていた

**判断**

部分修正ではなく全面的な再設計を行う。旧実装のコードとデータは参照しない。
リポジトリは履歴ごと削除して作り直す。参照可能な状態に置かないことで、
既存構造への引きずられを物理的に防ぐ。

**理由**

問題が個別の不具合ではなく、データモデルの設計に起因するため。特に
「図表がレコードとして存在しない」ことにより、検出精度を検証する手段自体が
存在しなかった。

---

## D002. 階層を自己参照の木で表現する

**関連**: SPEC 2.1、2.3、3.2

**背景**

原本の階層は最大8段。しかし段は頻繁に省略される。全ページの遷移を集計した
実測値は以下のとおり。

| 遷移 | 回数 | 省略される段 |
|------|------|-------------|
| `(２)` → `(a)` | 165 | 第5段 |
| `(２)` → `ア` | 35 | 第5・6段 |
| `３` → `(a)` | 20 | 第4・5段 |
| `３` → `ア` | 12 | 第4〜6段 |
| `ａ` → `(a)` | 5 | なし |

第5段 `ａ` を経由する形はわずか5回であり、`(２)` の直下に `(a)` が来るのが
標準である。

**判断**

`parent_id` + `seq` + `depth` による自己参照の木とし、深さの上限を設けない。
`marker_type` と `depth` は独立に保持し、一方から他方を導出しない。

**理由**

固定カラムでは深さの違いを吸収できない。旧実装は3カラムしか持たず、
第6段以下を失っていた。

段が任意に省略されるため、記号の種類から深さを計算することは原理的に
できない。

---

## D003. 第1段を根とし、単一の根を設けない

**関連**: SPEC 2.1

**判断**

第1段（`Ⅰ`〜`Ⅶ`）のノードをそれぞれ木の根とする。文書全体を表す単一の根は
設けない。

**理由**

文書全体を表すノードは原本に対応物を持たない。人工的な根を置くと、その
`marker` や `path` を定義する必要が生じるが、根拠がない。

参照解決も第1段の内部で完結する（D010）ため、第1段をまたぐ探索が不要である。

---

## D004. `【】` 見出しを階層ではなく属性とする

**関連**: SPEC 2.5

**背景**

`【】` で囲まれた見出しは428件。うち422件は直後のノードが `(N)` 形式で
始まっていた。例外6件はいずれも四角囲みラベルの誤検出であり、真の例外は
存在しないと考えられる。

同一の見出しが複数箇所に出現する。`【適 用】` は36箇所、`【通信の移管】` は
7箇所。ユニークな見出しは337種。

**判断**

`【】` 見出しは階層の一段ではなく、第4段 `SECTION` の属性（`heading`）とする。
独立したノードにしない。

**理由**

`【】` を階層に含めると、`(２)` が `【】` の子になり、番号体系の親子関係が
一段ずれる。原本の番号は `(２)` → `ａ` → `(a)` と続くため、間に別の段が
入ることはない。

旧実装は `【】` を `kind='H4'` として独立レコードにしており、見出しと本体が
切り離されていた。

**補足**

見出しは一意でないため、一覧表示では所属パスの併記が必須となる。

---

## D005. ラベルを部分木に継承する属性とする

**関連**: SPEC 2.6、3.4

**背景**

四角囲みのラベルは74箇所。すべて `後方乱気流管制方式` の1種類である。

後方乱気流管制方式は文書内の複数箇所で個別に規定されており、それらが
後方乱気流管制方式の規定であることを明示する目的で付与されている。

**判断**

ラベルはノード種別ではなく、`SECTION` に付与される属性とする。効力は
そのノードを根とする部分木全体に及ぶ。

種類は現状1つだが、モデル上は複数を扱えるものとする。

**理由**

`後方乱気流管制方式` のラベルが `ｂ` に付いている場合、その配下の `(a)` `ア`
`イ` および表もすべて後方乱気流管制方式の規定である。ノード自身の種別と
すると、この関係が表現できない。

旧実装は `kind='BOX_TITLE'` として種別にしていた。

**補足**

種類が1つしかないため、UIは汎用のラベル絞り込みではなく専用の導線とする
（SPEC 4.8）。汎用のフィルタにすると選択肢が1つだけのプルダウンになる。

**追記: 見出しとの共存**

p214 の `(７)` は、見出し `【インターセクション・デパーチャー等の間隔】` と
ラベル `後方乱気流管制方式` の両方を持つ。両者は独立した属性であり、
排他的ではない。

配置の順序は「見出し → ラベル → 階層記号」で固定である。逆順の例は
確認されていない。

なお pdfplumber はラベルの枠を複数の細い線分として返す（p214 では12個）。
個別の矩形をラベルとして扱うと検出が破綻するため、近接する線分をまとめる
処理が必要である。

---

## D006. 図表の所属を紙面位置ではなく参照で決める

**関連**: SPEC 2.7

**背景**

p200では、`(a)` の下に `ア` `イ` があり、その下に2つの表が印刷されている。
y座標順に処理すると表は `イ` に属することになるが、正しい所属は `(a)` である。
根拠は `(a)` の本文にある「次表の最低基準以上の間隔」という参照。

**判断**

図表の所属は本文中の参照によって決める。紙面上の位置を所属の根拠としない。
ただし兄弟内の順序は紙面上の出現順による。

参照によっても決まらない場合は確定不能として記録し、推測しない。

**理由**

図表は組版の都合でページ下部にまとめて配置されるため、直前の本文が所属先とは
限らない。y座標順に流し込む処理では、この種の誤りを構造的に避けられない。

**補足**

所属の決定には参照解決が必要であり、参照解決には図表ノードの存在が必要である
ため、構築は「図表ノード生成 → 参照解決 → 所属確定」の順で行う。

---

## D007. 波括弧を展開してテキストとして保持する

**関連**: SPEC 2.8

**背景**

管制用語には、共通部分を繰り返さないための波括弧レイアウトが用いられる。
p158の例では、同一の内容が邦文では `／` によるインライン選択、英文では
波括弧で書き分けられていた。

```
邦文: ★(滑走路〔番号〕の)LVP／LVPDは適用されています。
英文: (RUNWAY〔number〕) { LVP / LVP FOR DEPARTURE
                          or
                          LOW VISIBILITY PROCEDURE / ... } IN FORCE.
```

旧実装はこれを66枚の画像として切り出していた。

**判断**

波括弧は組版上の表現であり `／` と等価とみなす。展開してテキストとして保持し、
展開後の全文を検索対象とする。画像として保持してはならない。

**理由**

同一内容が両方の記法で書かれている実例があることから、波括弧に意味論的な
役割はない。

画像化すると検索できない。旧実装の66枚は、そのまま66箇所の検索漏れであった。

**補足**

原本の体裁を再現するため、展開前の構造（前置部・選択肢群・後置部）も保持する。

---

## D008. ページラベルを識別子として使わない

**関連**: SPEC 2.11、2.15

**背景**

ページ下部の表記は連番ではなく、そのページが属するセクションと、その中での
順序を表す。全384ページのフッタを集計した結果は以下。

| 第1段 | 書式 |
|-------|------|
| `Ⅲ` | `(Ⅰ)〜(Ⅵ)－N－N`（3成分） |
| それ以外 | `Ⅰ－N` 等（2成分） |

3成分形式では第1段が省略されるため、`(Ⅳ)`（第2段の記号）と `Ⅳ`（第1段の
記号）は別のものを指す。現状、重複は0件。

改正シナリオごとの変動は以下のとおり。

| 改正 | 順序 | 節番号 | 範囲記号 | 論理パス |
|------|------|--------|----------|----------|
| 分量の増減 | 変動 | 不変 | 不変 | 不変 |
| 節の末尾追加 | 変動 | 不変 | 不変 | 不変 |
| 第2段の末尾追加 | 変動 | 不変 | 不変 | 不変 |
| 節の途中挿入 | 変動 | 変動 | 不変 | 変動 |

**判断**

ページラベルは識別子・外部参照・永続キーとして用いない。構成要素に分解して
保持し、表示・改正作業の入力・構造の検証に用いる。

**理由**

最終成分（順序）はあらゆる改正で変動する。

**補足**

論理パスも版をまたいで不変ではない。節の途中挿入により後続の番号が繰り下がる
ため、同じパスが別の内容を指すようになる。版をまたぐ参照にはアンカーを用いる
（SPEC 2.13）。

---

## D009. ページラベルを構造の検証に用いる

**関連**: SPEC 2.14

**背景**

ページラベルは、原本自身が「このページはどのセクションに属するか」を宣言した
情報である。384ページ中348ページから取得できた（残りは白紙と章の扉）。

**判断**

構築した木とページラベルの整合を検証し、不一致を `validation_issues` に
記録する。自動的な修正は行わない。

**理由**

348ページ分の正解データが原本に印刷されていることになる。人手で正解データを
作る作業を、階層に関してはほぼ不要にできる。

この検証は改正後も有効である。番号が変動しても、ページラベルと木が整合すると
いう関係は常に成立する。

**補足**

自動修正を禁じるのは、検証を通すために構造を歪める処理が入ることを防ぐため。

---

## D010. 参照の省略された段を参照元から補完する

**関連**: SPEC 2.12

**背景**

本文中の条項参照68件を調査したところ、**すべてが第2段から始まっており**、
第1段を含むものは0件であった。

```
(Ⅳ)６(４)ｂ(b)ア    ← 第1段 Ⅲ が省略されている
```

**判断**

省略された段は、参照元ノード自身のパスと同じであるとみなす。探索は同一の
第1段の中で行う。

**理由**

ページラベルと同じ省略の規則が適用されていると考えられる。

**補足**

`(Ⅳ)` という第2段の記号は `Ⅱ` と `Ⅲ` の両方に存在する。参照元の第1段で
補完するため解決自体はできるが、原本が第1段をまたぐ参照を意図していた場合、
誤った箇所に解決される。この場合は未解決にならず、誤りが静かに残る。

実装後、解決された参照の妥当性を抜き取りで確認すること。

---

**追記: 先行参照からの引き継ぎ（D010を補正）**

当初「省略された段は参照元ノードのパスと同じ」としていたが、実例により
不十分と判明した。

p243（参照元 `Ⅲ/(Ⅳ)/3/(2)/c/(b)`）の本文に次の記述がある。

```
既にデータブロックがターゲットに付加されている航空機の場合は、
５(２)ｃ(b)又は(３)ｃ(b)によりデータブロックが当該機のものであることを確認する。
```

1つ目の参照は第3段 `５` を明示するが、2つ目は第4段から始まる。当初の規則では
第3段に参照元の `3` を補うことになるが、正しくは1つ目の参照の `5` を引き継ぎ、
`Ⅲ/(Ⅳ)/5/(3)/c/(b)` を指す。

**改めた規則**

省略された各段について、同一ノード内で先行する参照を出現順の逆順に走査し、
その段が明示されている最初の参照の値を用いる。いずれにも明示されていなければ
参照元ノードのパスから補う。段ごとに独立して解決する。

**逆順走査とした理由**

「直前の参照から引き継ぐ」では不十分である。直前の参照にその段が明示されて
いない場合があり、さらに遡る必要がある。逆順走査であれば、
`５(２)ｃ(b)、(３)ｃ(b)、６(１)ａ又は(２)ｂ` において、最後の `(２)ｂ` が
最初の `5` ではなく直前の `6` を引き継ぐことが自然に導かれる。

**引き継ぎ範囲を「同一の論理パスを持つノードの本文」とした理由**

当初「同一の本文」という語を用いたが、範囲が曖昧であった。文単位（句点区切り）
とする案も検討したが、原本において参照の省略が文の境界でリセットされる保証が
ない。

論理パスは文書内で一意である（SPEC 2.4）ため、これを範囲とすればノード単位で
明確に閉じる。実装上も、レコードが変われば引き継ぎ状態をリセットするだけで
よく、文の切り出しが不要になる。

**参照の境界**

`(２)ａ(a)及び(b)` の `(b)` は独立した参照として扱う。逆順走査により
第4段・第5段が先行参照から補完され、`(２)ａ(b)` として正しく解決される。

接続表現を境界として認識できないと、参照の分割を誤り、引き継ぎが連鎖して
複数の参照が壊れる。

**記録の強化**

参照解決は誤りが多いと予想されるため、`refs` に `seq_in_node`
`inherited_from` `resolution_note` を追加し、補完の根拠を残す。
引き継ぎで解決された参照は全件を検証の対象とする（SPEC 2.14）。

---

**追記2: 子孫への参照と範囲指定（D010をさらに補正）**

逆順走査の規則にも反例が見つかった。p254 の同一ページに、記号が同じで
解釈の異なる2つの参照が存在する。

| 参照元のパス | 参照 | 正しい解決先 |
|--------------|------|--------------|
| `Ⅲ/(Ⅳ)/5/(3)/b` | `(a)から(c)に掲げる事項` | `b` の子 `(a)(b)(c)` |
| `Ⅲ/(Ⅳ)/5/(3)/a/(c)/イ` | `(a)若しくは(b)の規定` | `a` の子 `(a)(b)` |

前者は子への参照、後者は祖先の兄弟への参照である。逆順走査のみでは前者を
`a` の子と誤って解決する。

**子孫判定の規則**

参照が明示する最も浅い段 S が、参照元ノードのパスに含まれるか否かで判別する。

- 含まれない → 子孫への参照。参照元のパスをそのまま前置し、補完しない
- 含まれる → 参照元の外への参照。省略された段を補完する

**この規則が成立する理由**

兄弟または上位の階層を指す場合、参照元と異なる値を示す必要があるため、
参照元のパスに含まれる段が必ず明示される。子孫を指す場合はその必要がない。

上表の後者では、参照元のパスに既に第6段 `(c)` が含まれており、参照の `(a)`
は同じ第6段の別の値を指している。前者では参照元のパスに第6段が存在しない。

**検討したが採らなかった案**

解決先の候補を複数生成し、木に照会して実在するものを採る方式を検討した。
しかしこの方式はデータに依存し、複数の候補が実在する場合に優先順位の妥当性を
保証できない。上記の規則は記号と参照元のパスのみで決定的に定まるため、
そちらを採用した。

**範囲指定**

`(a)から(c)` は3ノードを指す一つの参照である。`から` を接続表現として境界に
すると `(b)` が失われる。`及び` `又は` `若しくは` `並びに` は並列を表し
境界となるが、`から` のみ範囲を表す。

一つの参照が複数の解決先を持つため、`ref_targets` テーブルを新設した。

**循環参照の検証条件の修正**

当初「解決先が参照元自身・祖先・子孫である参照」を誤りの疑いとしていたが、
子孫への参照は正当である。条件から子孫を除外した。

---

## D011. 全文検索に転置索引を用いない

**関連**: SPEC 3.12

**背景**

本文の総量は約28万文字（UTF-8で約800KB）。

検索方式の比較。

| 方式 | 2文字検索 | 追加容量 | 備考 |
|------|-----------|----------|------|
| FTS5 + unicode61 | 不可 | 小 | 日本語が1トークンになる |
| FTS5 + trigram | **不可** | 2.5〜4MB | 3文字未満を検索できない |
| LIKE 走査 | 可 | なし | ランキングは自前 |
| 自前bigram索引 | 可 | 1〜2MB | 実装量が大きい |

**判断**

転置索引・全文検索拡張を用いず、`search_text` テーブルへの走査で検索する。
1文字以上の任意の部分文字列を検索できることを要件とする。

**理由**

管制用語には「誘導」「間隔」「承認」「高度」など2文字の語が多く、trigram では
検索できない。1文字での検索が必要になる可能性も否定できない。

本文総量が索引を要する規模ではない。走査で十分な速度が得られる。

旧実装はFTS5を使用していたが、日本語トークナイザの問題により検索が十分に
機能していなかった可能性が高い。

---

## D012. 図表の画像をファイルとして保持し、座標も保存する

**関連**: SPEC 3.5、3.6

**背景**

旧実装の画像は182ファイル・約2.3MB（中央値7KB）。

| 方式 | 利点 | 欠点 |
|------|------|------|
| ファイル + パス | 初回ロードが軽い、gitで差分管理可 | 参照切れの管理が必要 |
| DBにBLOB | 参照切れが起きない | sql.jsが全体をメモリに読む、gitで肥大化 |
| 実行時にPDFから描画 | ファイル管理が不要 | PDF全体の取得が必要、実装が複雑 |

**判断**

画像はファイルとして保持し、データベースにはパスと座標（bbox）を持つ。
ファイル名は座標から決定的に生成し、再構築で変化させない。

**理由**

BLOBはデータベースを再構築するたびに数MBがgitの履歴に積み上がる。
改正のたびに再構築する運用（D001の方針）と相性が悪い。

座標を必須とするのは3つの理由による。検出精度の検証に使える、画像を失っても
再生成できる、将来の実行時描画への移行が可能になる。

---

## D013. 用語定義を独立した種別とする

**関連**: SPEC 2.2.1、3.9

**背景**

用語定義は215件。すべて第1段 `Ⅰ` の `定義` の節にある。

用語名には括弧が付随し、215件中214件が括弧を持つ。残る1件は旧実装の
抽出エラー（`MDH)`）であり、原本の問題ではない。

括弧内の形態は一様でない。

| 形態 | 例 |
|------|-----|
| 英訳のみ | `アーク(Arc)` |
| 英訳と略称定義 | `移動開始時刻(Estimated off-block time。以下「EOBT」という。)` |
| 略語の展開 | `RVSM(Reduced vertical separation minimum)` |

**判断**

`TERM` を `SECTION` とは別の種別とする。括弧内は `paren_raw` として原文を
保持し、そこから抽出した `term_en` `abbrev` は空を許容する。

**理由**

内容の性質が本文と異なり、将来の機能追加（学習・出題機能）で別扱いする
可能性がある。閲覧時の表示は `SECTION` と同等でよい。

括弧内を一律に英語表記として扱うと、`以下「EOBT」という。` まで英語表記に
なる。`RVSM` のように用語名と展開形の向きが逆になる例もあるため、原文を
保持したうえで抽出結果を別に持つ。

抽出できない場合に値を作らせないのは、誤ったデータが静かに混入することを
防ぐため。214/215という比率であるから、空の件数が2桁になれば異常と判断できる。

---

## D014. 用例を用例文ごとにノード化する

> 用例の親に関する判断は D018 により置き換えられている。本項の「用例文ごとに
> 1ノードとする」という判断は有効である。

**関連**: SPEC 2.8

**背景**

`〔例〕` は187箇所。一つの `〔例〕` の下に複数の用例文が並ぶ場合がある。
用例文が複数行に折り返される場合、折り返し行と後続の用例文は行頭のx座標が
一致するため、x座標では区別できない。

**判断**

用例文ごとに1ノードとする。分割は文末のピリオドを手がかりとする。

**理由**

「Oshima」のような語で検索したとき、該当する用例文だけを結果に出せる。
ブロック全体が1ノードだと、どれが一致したのか分からない。

本文書においてピリオドは文末以外に用いられない（略語のピリオドが存在しない）
ため、この判定は信頼できる。

---

## D015. `注` の係り先判定を未決とする

**関連**: SPEC 2.16 U1、3.10

**背景**

`注` は187件。うち53件が番号を持ち（`注１` `注２`）、134件が番号を持たない。

当初、行頭のx座標が係り先の階層を示すと考えたが、誤りであった。p152の
`注` は x=112.9 で `★` と同じ値だが、実際には `(１)ａ` に係っており、
`★` は200pt下にあって無関係であった。

本文中から `注１` を参照する箇所は0件。参照解決の必要はない。

**判断**

係り先の判定規則を未決とする。`attached_to` に推定値を入れ、`confidence` と
判定根拠（`indent_x`）を保持して、後から再判定できるようにする。

**理由**

自動判定の精度が見込めない。判定できないものを確定させるより、判定できない
ことを記録するほうが安全である。

`confidence` が `high` でないものを人が確認する運用とする。

**補足**

`注` の扱いは全体の設計に影響しないため、優先度を下げて後回しとした。

---

## D016. Ⅴ章を当面対象外とする

**関連**: SPEC 2.16 U5

**背景**

Ⅴ章（管制書類様式記入要領）は旧実装で176件のレコードを持ち、様式・記入例と
いう他章と異なる構成を持つ。木モデルがそのまま適用できるか不明。

**判断**

当面索引の対象外とする。構造の定義および格納方法は別途定める。

**理由**

実運用上の重要度が低い。他章の設計を確定させることを優先し、必要になった
時点で別のルールとして開発する。

---

## D017. データベースを再構築可能な派生物とする

**関連**: SPEC 2.1、2.15

**判断**

データベースは原本から完全に再構築可能な派生物とし、利用者が作成した情報
（ブックマーク等）を含めない。

**理由**

改正が頻繁であり、そのたびに再構築する。利用者データを含んでいると、再構築の
たびに移行処理が必要になる。

改正の内容によっては部分再構築ができず全体を作り直すことになるが、派生物で
あれば作り直すことに副作用がない。

---

## D018. 用例の親を `PHRASE` から兄弟関係へ変更する

**関連**: SPEC 2.8、4.7。D014 の判断の一部を置き換える

**背景**

当初 `EXAMPLE` を `PHRASE` の子としていたが、p302 の実例により誤りと判明した。

```
エ 既知の場合は航空機の高度に関する情報及び型式
  ★トラフィック、〔数値〕時の方向…
  ★トラフィック、あなたの〔方位〕…
  ★トラフィック、〔位置〕の付近に多数のターゲット
  〔例〕（9文）
```

9つの用例文は3つの管制用語の全体に対応しており、特定の一つに紐づかない。
最後の用例文 `Traffic, numerous targets vicinity Tsuiki airbase.` は3つ目の
用語に対応する。直前の `PHRASE` に付けると、大半の用例が誤った所属になる。

同ページの `(b)` も2つの管制用語に対し用例3文という同じ構造であり、これが
標準的な形であると判断した。

**改めた規則**

`EXAMPLE` の親は `PHRASE` 群と同じ親とし、`PHRASE` の兄弟として並べる。
個々の用例文がどの管制用語に対応するかは判定しない。

当初の判断（D014本文）は p152・p157 の例に基づくものであった。これらは
1つの `〔例〕` が1つの `PHRASE` に対応していたため、より一般的な形を
見落としていた。

**UIへの影響**

用例が個々の用語に紐づかないため、管制用語一覧で特定の用語を選択した際、
同じ親を持つ管制用語と用例をすべて表示する（SPEC 4.7）。選択した用語のみを
表示すると、対応する用例が判別できない。

同じ親を持つ管制用語は状況に応じた選択肢の関係にあることが多く、併せて示す
ことが理解を助けるという利点もある。

**ページ境界**

用例はページをまたぐ（p302→p303）。継続部分に `〔例〕` の表記がないため、
ページを単位として処理すると継続を判定できない。

---

## D019. 階層記号の判定に「記号直後は空白」ルールを用いる

**関連**: SPEC 2.3、2.3.2、2.10。`src/structure.py` の `parse_marker_chain`

**背景**

行の折り返しにより、参照表記の一部が行頭に来ることがある。p200では
`((２)－５図)` が `((２)－` / `５図)` の2行に分割され、継続行の先頭「５」が
第3段（`number`）の記号パターンと一致してしまい、誤って新しいノードとして
検出された。

文字単位でPDFを調べたところ、本物の階層記号は必ず本文との間に実在の空白
文字を1つ挟んでいることが分かった（`ｂ␣`、`(a)␣`、`ア␣`、`イ␣` の形。
`␣`はU+0020の実測値）。一方、上記の「５図)」のような偽陽性は、記号の直後に
本文中の別の文字が空白なしで続く。

**判断**

行頭から取り出した記号の並び（2.3.2の連続記号を含む）の直後が、空白文字
または行末である場合に限り、階層記号として採用する。それ以外は継続行の本文
として扱う。

**検証**

底本384ページ全体（文字を持つ378ページ）を対象に、このルールが無かった場合に
誤検出となる候補を走査した。

| 指標 | 件数 |
|---|---|
| ルールにより継続行と判定した候補（誤検出を回避） | 848件 |
| ルール適用後も階層記号として採用した候補 | 2,775件 |

誤って継続行と判定してしまった実例（採用すべき記号を誤って除外した反例）は、
目視確認の範囲では見つからなかった。

p243の実例（`５(２)ｃ(b)又は(３)ｃ(b)により` の行またぎ）、p245の実例
（`(１)ａ`〜`(f)`、`ｂ(a)`、`ア(ア)`、`イ(ア)` 等の同一行併記・第8段）は
いずれも正しく処理できることを個別に確認した。

**不採用とした代替案**

「前の行が句点（。）で終わっていない場合は継続行とみなす」という代替ルールを
検討したが、同じ全ページ走査で検証した結果、p245の `(a)`〜`(f)`
（例：「管制間隔の設定上必要な場合」のように句点を伴わない列挙項目が並ぶ）で
後続の `(b)`〜`(f)` を軒並み継続行と誤判定することが分かった。SPEC 2.5の
「見出し→ラベル→階層記号」の順で、見出しやラベルの直後に階層記号が続く場合も
同様に句点なしの遷移となるため、この代替案は不採用とした。

**補足**

このルールは実測データから帰納した経験則であり、SPEC本文には明記されていない。
将来、記号直後に空白を伴わない本物の階層記号が見つかった場合（未検証の反例）は、
この記録を参照した上でルールを見直すこと。検証済みの範囲は
p199, p200, p214, p243, p245 および上記の全ページ走査（構造判定を伴わない
記号パターンの機械的な走査のみ）である。

---

## D020. 図番号の正規化で全角ハイフンも半角に変換する

**関連**: SPEC 2.10、2.12、3.5。`src/figures.py` の `normalize_figure_number`

**背景**

図のキャプション原文は `(２)－１` のように、全角数字と全角ハイフン（`－`、
U+FF0D）で構成される。SPEC 2.10 の正規化規則は「全角英数字を半角に統一する」
としか定めておらず、ハイフン（英数字ではない）の扱いに明示的な言及がない。

一方、本文中の図参照表記は `((２)－４図)` のように、図番号をそのまま埋め込む
形で現れる（p200実測）。stage8（参照解決）でこの参照表記から図番号を抽出し、
`figures.number_norm` と突き合わせて解決先を決める。

当初の実装は `_FULLWIDTH_TO_HALF`（数字・英字のみを変換するテーブル）を
そのまま流用しており、ハイフンが全角のまま残っていた
（`(2)－1` のように数字だけ半角化される中途半端な状態）。これ自体は誤りでは
ないが、参照解決側で同じ変換を独立に行うと、変換規則が2箇所に分散し、
将来どちらかだけ変更されて不一致を起こす懸念がある。

**判断**

`number_norm` はハイフンも含めて正規化する。`(２)－１` → `(2)-1`。
参照解決（`references.py`）が本文中の図参照表記を正規化する際も、同じ
`figures.normalize_figure_number` を用いて同一の変換規則を適用する。

**理由**

図番号は自然文ではなく識別子（半構造化データ）であり、突き合わせの対象は
「人間が読みやすい表記」ではなく「一致判定できること」である。ハイフンの
全角・半角が食い違ったまま突き合わせると、本来一致するはずの参照が
機械的に不一致になり、未解決件数だけが増える。SPEC 2.10 の「全角英数字を
半角に統一する」という規則の趣旨（表記揺れを検索・突き合わせの障害にしない）
をハイフンにも及ぼした。

**補足**

この判断はSPEC本文に明記されていない拡張であり、CLAUDE.md原則4に従い
ここに理由とともに記録する。図番号以外の一般本文（`nodes.text_norm`）に
同じ変換を適用するかどうかは別途の判断を要する。本文中のハイフンが
すべて図番号の一部とは限らないため、ここでは図番号の正規化とその参照抽出に
限定する。

---

## D021. 参照抽出（stage8）の誤検出防止規則

**関連**: SPEC 2.12。`src/references.py` の `_match_chain_at` / `_extract_section_spans`

**背景**

D019は行頭の階層記号抽出における誤検出防止規則を記録した。参照抽出
（`references.py`）は本文中の**任意の位置**から階層記号らしき並びを探すため、
行頭に限定するD019の防御（記号直後の空白）が使えず、別の誤検出パターンが
生じた。全384ページ（文字を持つページ）の`text_raw`を対象に、実際に生成
される参照候補を全数走査して確認した（`out/reference_span_scan3.txt`
`out/reference_span_scan4.txt`）。

判明した誤検出は3パターン。

1. **カタカナの連続語**：「ポイントアウト」「アプローチ」等、通常のカタカナ語が
   1文字ずつ`kana`型として一致し続け、見せかけの多段チェーンになる
2. **段の昇順違反**：上記1の亜種として、「３スキャン」のように数字の直後に
   無関係なカタカナ語が続く場合、`number`→`kana`という一見妥当な段の並びが
   偶然成立してしまう
3. **助数詞の小書きカタカナ**：「１ヵ月」の「ヵ」（小書きカタカナ）が`kana`型
   と誤って一致する。第7段の記号（ア、イ、ウ…）は通常サイズの文字のみで
   あり、拗音・促音・助数詞用の小書き文字（ァィゥェォッャュョヮヵヶ）が
   段記号として使われることはない
4. **「から」の前後で段が食い違う偶然の一致**：「レーダーサイトから１海里」の
   ように、「から」という一般的な語の前後にたまたま記号らしき文字（カタカナ1文字
   と数字）が来る場合に、範囲指定と誤認する

**判断**

以下の規則をすべて満たす場合に限り、記号の並びを参照候補として採用する。

- 連続する記号は、段の順位（SPEC 2.3.1のRANK）が**狭義単調増加**でなければ
  チェーンを打ち切る（誤検出2の防止。SPEC 2.3.2の同一行併記は常に浅い段→
  深い段の順であり、この制約は本物の併記を排除しない）
- 素の`kana`型（`(ア)`のような括弧付きではない）の直後にさらに全角カタカナ
  文字が続く場合、その`kana`型の一致を採用しない（誤検出1の防止）
- 小書きカタカナ（ァィゥェォッャュョヮヵヶ）は`kana`型として採用しない
  （誤検出3の防止）
- 「から」で連結する場合、前後の記号列の末尾セグメントの段が一致しない場合は
  「から」を境界として認めない（誤検出4の防止。SPEC 2.12「からの前後は同じ段
  の記号でなければならない」を、解決時の後付けチェックではなく採用条件として
  適用する）
- 単独の記号列（接続表現を伴わない）は、2段以上を含む場合のみ参照候補とする。
  1段のみの単独記号（例：本文中に単発で現れる「ａ」）は不採用とする
  （D019と同様、記号の偶然の一致を排除できないため）

**検証**

全ページ走査の結果、単独多段チェーン79件・接続表現で連結されたグループ86件、
計165件の参照候補が残った。目視確認した範囲では、いずれも実際の条項参照
として妥当な内容だった（例：`(Ⅳ)６(４)ｂ(b)ア`、`(a)から(c)`、
`(４)ａ、(５)ａ、(７)ａ及び(８)ａ`）。上記の誤検出3パターンはすべて解消を確認した。

**不採用とした代替案**

「単独1段の記号も、直後に『の規定により』等の定型句が続けば参照として採用する」
という案を検討したが、定型句の一覧を仕様から独立に列挙する必要があり、
CLAUDE.md原則4（推測で仕様の穴を埋めない）に反すると判断し見送った。実例が
見つかった時点で改めて判断する。

**補足**

この規則群はSPEC本文に明記されていない、実装上の経験則である（D019と同じ
位置づけ）。将来、単独1段の参照や小書きカタカナを含む本物の第7段記号が
見つかった場合は、この記録を参照した上で規則を見直すこと。

**追記2: text_norm切り替えに伴う再検証（D021を補正）**

**関連**: SPEC 2.10、2.12。`src/references.py` の `_match_chain_at` /
`_has_continuation`。D022・D023 とあわせて、`nodes.text_norm`（SPEC 2.10）を
実装し、`references.py` の抽出元を `text_raw` から `text_norm` に切り替えた
際の再検証。

**背景**

D021本文の規則群はすべて `text_raw`（原本表記）を対象に策定された。
`text_raw` では、`number`・`alpha` 型の階層記号パターンは全角文字のみに
一致するため、本文中の英数字（原本ではほぼ半角）とは形の上で衝突しなかった。
`text_norm` では SPEC 2.10 により英数字を半角に統一するため、この安全域が
失われる。

切り替え後、全384ページの `text_norm` を対象に、D021策定時と同じ方法で
参照候補を全数走査した（`out/reference_span_scan5.txt`）。結果は
section-ref候補187件（figure参照99件、table参照17件）。

D021本文の4パターン（カタカナ連続語、段の昇順違反、小書きカタカナ、
「から」の段不一致）はいずれも文字幅に依存しない誤検出であり、
再検証の結果、既存の防御規則を変更する必要はなかった（そのまま有効）。

一方、切り替えにより新たに2パターンの誤検出が生じることを確認した。

1. **半角英字語への衝突**：p80「〔例〕...at 700feet, maximum thrust
   required.」の `700feet` が、数字型「700」→alpha型「f」という段の昇順を
   満たすチェーンとして誤って一致し、「700f」という見せかけの参照になった。
   `text_raw` の時点では alpha型が全角のみに一致していたため、英文中の
   半角小文字とは衝突しなかった。
2. **カタカナ語末尾への衝突（後方チェックの欠落）**：p88「単位は5,000
   メートルを超える場合はキロメートル、5,000メートル以下の場合は
   メートルを使用する。」の「メートル、5,000」が、「ル」（kana型、
   「メートル」の末尾文字）→「、」（境界表現）→「5」（number型、
   「5,000」の先頭）という2連結チェーンとして誤って一致し、「ル、5」と
   いう見せかけの参照になった。D021本文のカタカナ連続語チェックは
   一致箇所の**直後**（例：「メ」の直後の「ー」）しか見ておらず、
   一致箇所が語の**末尾**である場合（「ル」の直前の「ト」）を見逃す
   欠陥が、`text_raw` では number型が半角「5,000」に一致しなかったために
   これまで表面化していなかった。

**判断**

以下の2規則を追加する。

- alpha型の一致箇所の直後・直前がともに半角**小文字**英字である場合、
  その一致を採用しない（誤検出1の防止）。大文字は対象に含めない
  （後述）
- kana型の一致箇所の直後だけでなく**直前**についても、既存の
  `_KANA_CONTINUATION`（カタカナ・長音符）と同じ判定を行う（誤検出2の
  防止。D021本文の前方チェックを前後両方向に拡張する）

**大文字を対象外とする理由**

alpha型のチェックを大文字にも広げると、本物の参照を誤って除外する。
p75実測：「(３)ATISａ(a)アからオに掲げる事項」の「ａ」（本物の階層記号。
第4運航情報業務処理規程の節を指す条項参照の一部）は、直前が英大文字の
頭字語「ATIS」の末尾「S」である。大文字から小文字への切り替わり自体が
語の境界を示しており、「ATIS」と「ａ」は同じ語の続きではない。この
規則を大文字・小文字を区別せず適用した結果、実際にこの参照の先頭
セグメントが誤って除外される回帰を一度確認した。小文字のみを対象とする
ことで、誤検出1（"700feet"は小文字のみ）は防ぎつつ、この回帰を避けた。

**検証**

修正前後で候補件数は189件→187件（figure・table参照を含まないsection-ref
候補は189→187、内訳はALPHA系113→112、非ALPHA系76→75）。差分は上記の2件の
誤検出のみであり、それ以外の185件（既存の妥当な参照）は変化していないことを
1件ずつ確認した。

**補足（残存する既知のリスク。今回のスコープ外）**

p89「１から９までの滑走路番号の場合は」（`text_norm`では「1から9」）、
p331「別表(２)を参照して1、2及び3の...」のような、通常の日本語表現として
数字の範囲・列挙を述べているだけの箇所が、`から`・`及び`・`、` を境界とする
section参照の構文と区別できず、引き続き誤って候補になる。これは
`text_raw` の時点で既に全角数字だったため、`text_norm` への切り替えとは
無関係な、D021策定時点から存在した残存リスクである（策定時の165件には
含まれていなかった可能性があり、当時の目視確認から漏れていたと考えられる）。
文字クラスの一致だけでは、本物の条項参照と「同じ段の記号がたまたま
「から」で結ばれた日常表現」を区別できないため、今回は対応を見送る。
将来、この種の誤検出が参照解決の品質に無視できない影響を与えると分かった
場合は、別途ガードを検討すること。

**補足2（`_has_continuation` が構造的に防げない残存パターン）**

`_has_continuation` の alpha側チェックは、一致した半角小文字の**直後・直前**
にさらに半角小文字が続く場合のみ「単語の一部」と判定する。そのため、数字に
単一の小文字（単位の略号1文字など。例：仮に「700m」のような表記が本文中に
現れた場合）が直接続き、かつその小文字の直後・直前がどちらも英字でない
ケースは、前方・後方どちらのチェックにも掛からず、number→alpha の段の
昇順を満たすチェーンとして誤って候補になりうる。

全384ページの `text_norm` を対象に、この形（半角数字列の直後に半角小文字が
1文字だけ続き、その前後は英字でない）を機械的に走査したが、該当は0件
だった。原本の実際の単位表記（メートル、フィート、ノット等）はすべて
片仮名で書かれるか、英語表記でも2文字以上の単位（`feet`、`knots`等）で
あるため、この形は現状の底本には存在しない。

したがって現時点でのガードの正しさは、コード上の構造的な保証ではなく
「この底本にはこの形の実例が無い」という走査結果に依存している。改正で
単一文字の単位略号（例：分を表す`m`等）が数字に直接続く表記が追加された
場合、この保護は機能しない。CLAUDE.md原則4（推測で仕様の穴を埋めない）に
従い、実例が無い段階でこれ以上厳格なガード（例：number型の直後に
alpha型が続くこと自体を一律禁止する等）を先回りして追加することは
見送った。実例が見つかった時点で改めて対応すること。

---

## D022. text_norm におけるハイフン類の半角統一（D020の適用範囲拡張）

**関連**: SPEC 2.10、2.12。`src/structure.py` の `_HYPHEN_LIKE_TO_HALF`、
`normalize_text`。D020（`figures.normalize_figure_number` のハイフン処理）
を書き換えず、本記録から参照する形で適用範囲を広げる。

**背景**

SPEC 2.10「正規化規則」は「全角英数字を半角に統一する」とのみ定める。
ハイフンは英数字ではないため、本文全体の正規化テキスト（`nodes.text_norm`）
でハイフンを変換すべきかどうかは、SPEC本文が沈黙している論点である
（D020の補足に「図番号以外の一般本文に同じ変換を適用するかどうかは別途の
判断を要する」と記載されていた、その判断にあたる）。

ハイフンを変換しない場合、D020で図番号専用に半角化した `figures.number_norm`
（例：`(2)-4`）と、一般本文の `text_norm` に残る全角ハイフン（例：
`((2)－4図)`）との間に表記の非対称が生じる。検索が LIKE ベースである以上、
利用者が半角ハイフンで図番号を検索すると、本文中の参照表記側では全角の
ままヒットしない。

ハイフンに類似する文字が原本に他にも使われていないかを全384ページで
走査した（`out/dash_scan.txt`）。結果：

| 文字 | 件数 | 備考 |
|---|---|---|
| U+FF0D（全角ハイフンマイナス） | 1295 | 図番号キャプション・参照はすべてこれ |
| U+002D（ASCIIハイフン） | 119 | すべて英単語内（`ADS-C`等）。図参照ではない |
| U+2013（EN DASH） | 1 | p35、英語の用語説明内。図参照ではない |
| U+2010（ハイフン） | 1 | p212、本文中の図参照表記「(４)‐４図」で使用 |
| その他（U+2011/2012/2014/2212等） | 0 | 出現なし |

p212の1件は、図(４)－４のキャプション自体（p207）は通常どおり U+FF0D で
あるにもかかわらず、それを参照する本文側だけが U+2010 になっている。
原本の表記揺れ（誤植またはフォント代替）と判断した。U+FF0D のみを変換
対象とすると、この1件だけ参照が解決できず未解決として記録される。

**判断**

`text_norm` でも、区切りに使われるハイフン類を半角に統一する。対象は
次の2文字のみ：

- U+FF0D（全角ハイフンマイナス）
- U+2010（ハイフン。p212の1件のみだが、図参照としての役割が明白なため
  含める）

対象外（変換しない）：

- U+30FC（カタカナ長音符「ー」）。ハイフンではなく、変換すると
  「レーダー」等の通常の語を破壊する
- U+002D（ASCIIハイフン）。既に半角であり変換不要
- U+2013（ENダッシュ）。実測1件のみで、図参照としての用法が確認できない
  （英語の用語説明内での使用）。対象に含める根拠がない

比較を行う関数を `figures.normalize_figure_number` の1つに限定し、
`references.py` の図参照抽出（`_find_figure_refs`）は、`text_norm` から
抜き出した数字部分を必ずこの関数に通してから `FIGURE.number_norm` と
突き合わせる。`text_norm` は生成時点で既に半角化済みのため、この呼び出しは
現状は恒等変換になるが、将来どちらかの正規化規則が変わっても突き合わせ
箇所が分散しないようにするため、呼び出し自体は残す。

**理由**

「全角英数字を半角に統一する」はハイフンについて何も述べていない。
ハイフンを変換しないことは SPEC 本文への忠実な実装ではなく、沈黙に
対する一つの解釈にすぎない（変換する場合も同様に解釈が必要であり、
どちらを選んでも「仕様の穴」を埋める判断になる）。検索用途
（`text_norm` の目的、SPEC 2.10）を重視し、図番号という主要な参照手段が
半角ハイフンの入力で空振りしない設計を優先した。

**補足（仕様の穴として残す）**

SPEC 2.10 はハイフンの扱いについて沈黙している。将来 SPEC を改訂する際は、
「区切り記号としてのハイフン類は英数字と同様に半角へ統一する」旨を
明記することを候補として検討されたい。

`search.py`（stage10、未実装）でユーザーの検索クエリを `search_text` と
突き合わせる際も、クエリ側に同じハイフン正規化を適用する必要がある。
未適用のまま実装すると、半角ハイフンで図番号を検索したときだけ
ヒットし、全角ハイフンで検索するとヒットしないという非対称が生じる。
search.py 実装時に確認すること。

---

## D023. 段落区切りの改行（SPEC 2.10）はno-opとして実装する

**関連**: SPEC 2.10。`src/structure.py` の `normalize_text`。

**背景**

SPEC 2.10 は「段落の区切りとしての改行は保持する」と定める。しかし
`nodes.text_raw` は `structure.build_forest` の行組み立て時点で、既に
折り返し・段落を区別せず単純連結された一続きの文字列である
（`current_leaf.text_raw += line.text` / `+= remainder`。空行は
読み飛ばされる）。そのため、`text_raw` の時点で段落区切りの情報は
そもそも保持されていない。

この規則を `text_norm` 生成の段階で実装する意味があるかどうかを判断する
ため、底本384ページ全体を対象に、同一ノードの継続行として連結される
2行の間に、通常の行間より大きい垂直方向のギャップ（段落区切りを示唆する
空行）が存在するかを走査した（`out/paragraph_break_scan.txt`）。

判定は、ページ内の行間隔の中央値の2倍を超え、かつ6pt を超えるギャップを
候補とし、見出し・階層記号・NOTE・PHRASE・図キャプション単独行を起点/
終点に持つペア（＝別ノードへの遷移であり段落区切りではない）を除外する
方法による。

該当件数62件。目視確認した結果、すべて次のいずれかであり、SPEC
2.1〜2.9が対象とする階層構造本文（本編）の内部には1件も無かった。

- 目次（p7〜p20）の項目間
- 巻末の付録・様式（p341〜p383）の項目間
- フッター・ページラベルの走査漏れ（後述の別件を参照）
- 図キャプション同士の間隔

**判断**

`normalize_text` では、段落区切りの改行保持について能動的な処理を
行わない（no-op）。ただし「規則が存在しない」のではなく「この底本の
本編には該当する実例が存在しないため、対応するコードが無くても規則を
満たす」という位置づけで実装する。

**理由**

実例が無い規則を先回りで実装すると、根拠のない設計（テストで検証
できない分岐）を持ち込むことになる（CLAUDE.md「推測で仕様の穴を
埋めない」）。`text_raw` の生成方法自体を変更しない限り、段落区切りを
表現する手段が無い以上、`text_norm` 側だけで対応することもできない。

**補足**

改正により本編の内部に段落区切りを持つ項目が追加された場合、この
判断は成り立たなくなる。その場合はまず `structure.build_forest` の
行組み立て（空行の扱い）を見直す必要があり、`text_norm` の課題ではなく
`text_raw` 生成（処理順序表 #3）の課題になる。

---

## 記録の書き方

新しい判断を追加する際は、以下を含めること。

- **背景**: 判断の前提となった事実。実測値があれば数値で
- **判断**: 何をどう決めたか
- **理由**: なぜそう決めたか。特に他の選択肢を採らなかった理由
- **補足**: 副次的な影響、注意点、将来の課題

判断が覆った場合は、古い記録を書き換えず、新しい記録に「D0NNを置き換える」と
明記する。
