# 黒本デジタル化システム 実装ガイド

このファイルはセッション開始時に自動で読まれる。作業を始める前に必ず目を通すこと。

---

## 絶対に守ること

1. **`SPEC.md` を編集しない。** 実装と仕様に相違がある場合は、実装を修正する。
   仕様側に誤りがあると考えるときは、変更せず報告して指示を待つ。

2. **旧実装を参照しない。** 不完全な旧実装が過去に存在したが、リポジトリの
   履歴ごと削除されている。本プロジェクトは一から設計されており、旧実装に
   合わせるべきものは何もない。断片を見つけた場合は参照せず報告すること。

3. **検証を通すために判定を緩めない。** 検証で不一致が出た場合、原因は実装に
   ある。検証条件を書き換えて通すことは、問題を隠すことと同じである。

4. **推測で仕様の穴を埋めない。** 仕様に書かれていない判断が必要になったら、
   作業を止めて報告する。実装で埋めると、後から発見できなくなる。

5. **確定できないものを確定させない。** 図表の所属、注の係り先など、判定できない
   場合は「確定不能」として記録する。もっともらしい値を入れてはならない。

---

## 環境

- Python 3、pdfplumber、SQLite
- 底本: `pdfs/` 配下のPDF（384ページ）
- 出力: `data/index.db`、`figures/`、`out/`

パッケージの追加が必要な場合は、理由とともに報告してから導入する。

---

## ファイル構成

```
SPEC.md              仕様。編集禁止
CLAUDE.md            本ファイル
DECISIONS.md         判断の記録。人間向けであり、実装時に読む必要はない

src/
  build.py           構築の入口。以下を順に呼ぶ
  extract.py         PDF抽出、領域分割（表→囲み→図→波括弧）
  structure.py       行の組み立て、階層記号の抽出、木の構築
  attributes.py      見出し、ラベル、ページラベルの付与
  figures.py         図表ノードの生成
  references.py      参照の解決、図表の所属確定
  search.py          search_text の生成
  validate.py        検証
  schema.sql         スキーマ定義

data/index.db        構築されるデータベース
figures/             切り出した図の画像
pdfs/                底本
out/                 検証レポート、中間結果
web/index.html       閲覧画面
```

モジュールの分割は「処理の順序」（後述）に対応している。
一つの巨大なスクリプトにしないこと。

## コマンド

```bash
python src/build.py              # 全体を構築
python src/build.py --page 200   # 単一ページのみ処理（開発用）
python src/build.py --stage 5    # 指定段階まで実行して中間結果を保存
python src/validate.py           # 検証を実行し out/report.html を出力
```

---

## 進め方

- **段階的に広げる。** いきなり384ページを処理しない。
  1ページ → 1節 → 1章 → 全体の順に対象を広げる
- **中間結果を保存する。** 各段階の出力をファイルに残し、どこで壊れたかを
  特定できるようにする
- 大きな設計変更の前に方針を述べ、確認を取る
- 検証の数値が悪化したら、原因を報告して止まる。先に進まない

### 開発時の起点

`p200`（Ⅲ/(Ⅲ)/3 の範囲）を最初の対象とする。このページには、
階層の併記・四角囲みラベル・図表の所属・段の省略がすべて含まれており、
主要な難所を一度に検証できる。

次に以下を対象とする。

| ページ | 含まれる難所 |
|--------|-------------|
| `p158` | 管制用語、波括弧レイアウト |
| `p245` | 第8段、多重併記 |
| `p302`〜`p303` | 用例の親、ページまたぎ、`〔例〕`1文目の同一行問題 |
| `p254` | 参照の子孫判定、範囲指定（`から`） |

---

## 処理の順序

以下の順序を守る。順序を入れ替えると解けない依存がある。

| # | 処理 | モジュール |
|---|------|-----------|
| 1 | テキスト・図形要素の抽出 | `extract.py` |
| 2 | 領域分割（表 → 囲み → 図 → 波括弧） | `extract.py` |
| 3 | 行の組み立て | `structure.py` |
| 4 | 階層記号の抽出 | `structure.py` |
| 5 | 木の構築 | `structure.py` |
| 6 | 属性の付与（見出し、ラベル、ページラベル） | `attributes.py` |
| 7 | 図表ノードの生成 | `figures.py` |
| 8 | 参照の解決 | `references.py` |
| 9 | 図表の所属確定 | `references.py` |
| 10 | 検索用テーブルの生成 | `search.py` |
| 11 | 検証 | `validate.py` |

**7〜9の順序は仕様上の要請である**（SPEC 2.7）。図表の所属は参照によって
決まり、参照の解決には図表がノードとして存在している必要がある。

**2の順序も守ること。** 先に確定した領域は、後続の検出から除外する。
除外しないと同じ領域が二重に処理される。

---

## 落とし穴

### 行の組み立て

**y座標だけで行を作ってはならない。** 波括弧を含むページでは、括弧の内側と
外側が同じy座標に存在する。y座標で行を作ると次のように連結される。

```
実際:   EXPECT VECTOR （括弧の外・共通部）
        approach segment / intermediate fix〕. （括弧の内・選択肢の3行目）

誤結果: EXPECT VECTOR approach segment / intermediate fix〕.
```

x座標で領域を分割してから、領域ごとに行を組むこと。

### 階層記号の抽出

**行頭の記号は1つとは限らない。** 親が固有の本文を持たない場合、記号が並記される。

```
(１)ａ レーダー誘導(以下「誘導」という。)は…    → (１) の下に ａ
ｂ(a) 最低誘導高度は、地形、レーダー性能…       → ｂ の下に (a)
ア(ア) 空港監視レーダーのレーダーサイトから…    → ア の下に (ア)
```

行頭から記号を1つだけ取ると、必ず親か子を取りこぼす。連続する記号をすべて
取り出し、段の順序（SPEC 2.3.1）で親子関係を判定すること。
3つ以上の並記や、段を飛ばした並記の可能性も想定する。

### x座標を階層の根拠にしない

同じ第8段 `(ア)` が、併記の有無によって x=118 にも x=129 にも現れる。
`注` の行頭x座標が `★` と一致することもあるが、両者は無関係である。

x座標は補助的な手がかりにとどめ、階層の判定は記号の種類と出現順で行う。

### 図表の所属

**y座標順に本文へ流し込むと、必ず所属を誤る。**

p200では、2つの表がページ最下部（`イ` の本文より下）に印刷されているが、
論理的には `(a)` の子である。`(a)` の本文に「次表の最低基準以上の間隔」と
あるためである。

紙面上の位置は所属の根拠にならない（SPEC 2.7）。ただし**兄弟内の順序は
紙面上の出現順による**。この2つを混同しない。

### 見出しとラベルの共存

`(N)` の直前に、見出しとラベルが両方置かれることがある（p214）。
どちらか一方を見つけたら他方を探さない、という処理をしてはならない。

配置は「見出し → ラベル → 階層記号」の順で固定である（SPEC 2.5）。

### ラベルの矩形は分解されて取得される

pdfplumber の `rects` は、1つの枠を複数の細い線分として返す。
p214 のラベル枠は12個の矩形として取得され、実体は次の1つである。

```
x0=70.9  x1=166.4  top=589.6  bottom=603.6
```

個別の矩形をラベルとして扱うと検出が破綻する。近接する線分を1つの枠に
まとめてから判定すること。ページ上の全矩形を1つのbboxに統合するのも誤りで、
空間的な近接によるクラスタリングが必要である。

### 領域の二重処理

表として確定した領域は、囲みの検出からも図の検出からも除外する。
除外しないと、同じ罫線が表としても図としても検出される。

### 日本語テキストのトークン幅

日本語には分かち書きがないため、pdfplumber が返すトークンが1行分の幅を
持つことがある。ある要素が別の要素に含まれるかを判定する際、x方向の厳密な
包含で判定すると失敗する。**y範囲の重なりで判定すること。**

### 空白の扱い

原本には均等割り付けの空白が含まれる（`【適 用】` `２ 定 義`）。
正規化テキストでは除去するが（SPEC 2.10）、**原文テキストでは保持する**。
両方を持つこと。

階層記号と本文の間の空白は除去しない。記号の切り出しに必要である。

### 白紙ページ

文字要素を1つも持たないページ、および文字が数個しかないページが存在する。
ページラベルも取得できない。異常として扱わず、正常系として処理すること。

### 管制用語と用例

**用例は管制用語群の全体に対応する。** 直前の1つに紐づけてはならない。
p302 では3つの `★` に対し `〔例〕` が9文あり、最後の1文は3つ目の用語に
対応する。`EXAMPLE` は `PHRASE` の子ではなく兄弟とする（SPEC 2.8）。

**用例はページをまたぐ。** p302 末尾の `〔例〕` は p303 冒頭まで続く。
継続部分に `〔例〕` の表記はない。ページ単位で処理すると継続を判定できず、
用例が失われるか誤った所属になる。

**`〔例〕` の1文目は同じ行にある。** pdfplumber では次のように分離して
取得されるため、そのまま行を組むと1文目が壊れる。

```
y=737 x0=128.6  〔例〕Traffic,
y=738 x0=197.9  12 o'clock, 10 miles, southbound.
```

y座標が1ptずれているだけで、実際には同一行である。

### 参照の抽出と解決

参照解決は本プロジェクトで最も誤りが生じやすい処理である。SPEC 2.12 を
必ず読んでから着手すること。

**接続表現は境界だが、`から` は違う。**

```
(２)ａ(a)及び(b)     → 2つの参照
(a)から(c)           → 1つの参照（範囲）。(a)(b)(c) の3ノードを指す
```

`から` を境界として分割すると `(b)` が失われる。

**同じ記号でも参照元により解決先が変わる。** p254 には次の2つが同居する。

| 参照元 | 参照 | 解決先 |
|--------|------|--------|
| `Ⅲ/(Ⅳ)/5/(3)/b` | `(a)から(c)` | 自身の子 |
| `Ⅲ/(Ⅳ)/5/(3)/a/(c)/イ` | `(a)若しくは(b)` | 祖先の兄弟 |

判定は、参照が明示する最も浅い段が参照元のパスに含まれるか否かによる。
含まれなければ子孫への参照であり、補完を行わない。

**参照は出現順に処理する。** 省略された段は、同一ノード内の先行する参照を
逆順に走査して補完する。各参照を独立に解決してはならない。

**段ごとに独立して解決する。** ある参照が第3段を先行参照から、第1段・第2段を
参照元から補うことがありうる。

引き継ぎは同一ノードの本文内に限る。ノードが変わったら引き継ぎ状態をリセット
すること。

補完の根拠は `refs.resolution_note` に必ず記録する。後から人が妥当性を
判断できる形で残す。

### PDF.js のバージョン

フロントエンドでPDFを表示する際、API と worker のバージョンが一致していないと
描画が無言で失敗する。エラーも出ない。両者を同一バージョンに固定すること。

---

## 検証

`validate.py` は SPEC 2.14 の自己検証を実行し、結果を `validation_issues`
テーブルと `out/report.html` に出力する。

### 何を見るか

| 指標 | 意味 |
|------|------|
| `validation_issues` の error 件数 | 構造の誤り。0を目指す |
| 未解決の参照件数 | 参照解決の精度 |
| 所属が確定不能な図表の件数 | 図表処理の精度 |
| `confidence` が `high` でない注の件数 | 人が確認すべき件数 |
| 引き継ぎで段を補完した参照の件数 | 誤りが連鎖しやすいため全件確認 |
| 解決先が参照元自身・祖先である参照 | 循環参照。抽出の誤りの可能性が高い |
| 範囲指定の展開結果が0件または1件 | 範囲の解釈または抽出の誤り |
| ページをまたぐノードの件数 | 少なすぎる場合、境界で誤って分断している |

**これらの件数が0でないことは、構築の失敗を意味しない。** 判定できないものを
記録しているだけである。件数が想定より多い、または前回より悪化した場合に
原因を調べる。

### ページラベルは正解データである

ページ下部の表記は、原本自身が「このページはどのセクションに属するか」を
宣言したものである。348ページ分の正解が印刷されていることになる。

構築した木とページラベルが食い違う場合、**ほぼ確実に木の側が誤っている**。

### 目視による確認

図表の検出は数値だけでは判断できない。PDFページの画像に検出したbboxを重ねた
コンタクトシートを `out/` に出力し、自分で見て確認すること。

---

## 報告の仕方

作業の終わりに、次を報告する。

- 何を変更したか
- 検証の数値が前回からどう変化したか
- 判断に迷った箇所と、どう処理したか
- 仕様に書かれていないために推測が必要だった箇所（あれば必ず報告する）

**数値が改善したことだけを報告しない。** 悪化した項目があれば必ず含める。

---

## 参照

| 内容 | 参照先 |
|------|--------|
| データモデル、ノード種別、階層 | SPEC.md 2章 |
| 図表の所属の決定順序 | SPEC.md 2.7 |
| 管制用語と波括弧の展開 | SPEC.md 2.8 |
| 正規化規則 | SPEC.md 2.10 |
| ページラベル | SPEC.md 2.11 |
| 参照の境界・省略段の補完 | SPEC.md 2.12 |
| 自己検証 | SPEC.md 2.14 |
| データベーススキーマ | SPEC.md 3章 |
| 画面と機能 | SPEC.md 4章 |
| 未決事項 | SPEC.md 2.16、3.17、4.13 |
